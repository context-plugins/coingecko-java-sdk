/*
 * CoinGeckoDemoApiLib
 *
 * This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
 */

package com.coingecko.api.apis;

import com.coingecko.api.ApiHelper;
import com.coingecko.api.Server;
import com.coingecko.api.exceptions.ApiException;
import com.coingecko.api.http.request.HttpMethod;
import com.coingecko.api.http.response.ApiResponse;
import com.coingecko.api.models.AssetPlatform;
import com.coingecko.api.models.CategoriesList;
import com.coingecko.api.models.Category1;
import com.coingecko.api.models.CoinsContractAddress;
import com.coingecko.api.models.CoinsId;
import com.coingecko.api.models.CoinsIdHistory;
import com.coingecko.api.models.CoinsIdTickers;
import com.coingecko.api.models.CoinsList;
import com.coingecko.api.models.CoinsMarket;
import com.coingecko.api.models.CoinsMarketChart;
import com.coingecko.api.models.Currency;
import com.coingecko.api.models.Days;
import com.coingecko.api.models.DerivativesExchange;
import com.coingecko.api.models.DerivativesExchangesId;
import com.coingecko.api.models.DerivativesExchangesList;
import com.coingecko.api.models.DerivativesTicker;
import com.coingecko.api.models.DexPairFormat;
import com.coingecko.api.models.DexesList;
import com.coingecko.api.models.Duration;
import com.coingecko.api.models.EntitiesList;
import com.coingecko.api.models.Entity;
import com.coingecko.api.models.EntityType;
import com.coingecko.api.models.Exchange1;
import com.coingecko.api.models.ExchangeRates;
import com.coingecko.api.models.ExchangesId;
import com.coingecko.api.models.ExchangesList;
import com.coingecko.api.models.Filter;
import com.coingecko.api.models.Global;
import com.coingecko.api.models.GlobalDeFi;
import com.coingecko.api.models.Include;
import com.coingecko.api.models.Include2;
import com.coingecko.api.models.Include3;
import com.coingecko.api.models.IncludeTickers;
import com.coingecko.api.models.IncludeTokens;
import com.coingecko.api.models.Interval;
import com.coingecko.api.models.Locale;
import com.coingecko.api.models.MultiPoolAddressData;
import com.coingecko.api.models.MultiTokenData;
import com.coingecko.api.models.NetworksList;
import com.coingecko.api.models.NfTsList;
import com.coingecko.api.models.NftData;
import com.coingecko.api.models.Ohlcv;
import com.coingecko.api.models.OnchainSimplePrice;
import com.coingecko.api.models.Order;
import com.coingecko.api.models.Order1;
import com.coingecko.api.models.Order2;
import com.coingecko.api.models.Order3;
import com.coingecko.api.models.Order4;
import com.coingecko.api.models.Order5;
import com.coingecko.api.models.Order6;
import com.coingecko.api.models.Order7;
import com.coingecko.api.models.PingServer;
import com.coingecko.api.models.Pool;
import com.coingecko.api.models.PoolAddressData;
import com.coingecko.api.models.PoolSearch;
import com.coingecko.api.models.PoolTokensInfo;
import com.coingecko.api.models.Precision;
import com.coingecko.api.models.PublicTreasuryEntity;
import com.coingecko.api.models.PublicTreasuryEntityChart;
import com.coingecko.api.models.PublicTreasuryTransactionHistory;
import com.coingecko.api.models.Search;
import com.coingecko.api.models.SimplePrice;
import com.coingecko.api.models.Sort;
import com.coingecko.api.models.Sort2;
import com.coingecko.api.models.Status;
import com.coingecko.api.models.Timeframe;
import com.coingecko.api.models.TokenData;
import com.coingecko.api.models.TokenInfo;
import com.coingecko.api.models.TokenInfoRecentlyUpdated;
import com.coingecko.api.models.TokenLists;
import com.coingecko.api.models.Trades;
import com.coingecko.api.models.TrendingSearch;
import com.coingecko.api.models.containers.ExchangeVolumeChart;
import com.coingecko.api.models.containers.PublicTreasury;
import com.fasterxml.jackson.core.type.TypeReference;
import io.apimatic.core.ApiCall;
import io.apimatic.core.GlobalConfiguration;
import io.apimatic.coreinterfaces.http.request.ResponseClassType;
import java.io.IOException;
import java.util.List;
import java.util.Map;
import java.util.concurrent.CompletableFuture;
import java.util.concurrent.CompletionException;

/**
 * This class lists all the endpoints of the groups.
 */
public final class Api extends BaseApi {

    /**
     * Initializes the controller.
     * @param globalConfig    Configurations added in client.
     */
    public Api(GlobalConfiguration globalConfig) {
        super(globalConfig);
    }

    /**
     * To check the API server status.
     * @return    Returns the PingServer wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PingServer> pingServer() throws ApiException, IOException {
        return preparePingServerRequest().execute();
    }

    /**
     * To check the API server status.
     * @return    Returns the PingServer wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PingServer>> pingServerAsync() {
        try {
            return preparePingServerRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for pingServer.
     */
    private ApiCall<ApiResponse<PingServer>, ApiException> preparePingServerRequest() {
        return new ApiCall.Builder<ApiResponse<PingServer>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/ping")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PingServer.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the prices of one or more coins by using their unique Coin API IDs, symbols, or
     * names.
     * @param  vsCurrencies  Required parameter: Target currency of coins, comma-separated if
     *         querying more than 1 currency. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies)
     * @param  ids  Optional parameter: Coins' IDs, comma-separated if querying more than 1 coin.
     *         *refers to [`/coins/list`](/reference/coins-list)
     * @param  names  Optional parameter: Coins' names, comma-separated if querying more than 1
     *         coin.
     * @param  symbols  Optional parameter: Coins' symbols, comma-separated if querying more than 1
     *         coin.
     * @param  includeTokens  Optional parameter: For `symbols` lookups, specify `all` to include
     *         all matching tokens. Default `top` returns top-ranked tokens by market cap or volume.
     * @param  includeMarketCap  Optional parameter: Include market capitalization. Default: false
     * @param  include24HrVol  Optional parameter: Include 24-hour trading volume. Default: false
     * @param  include24HrChange  Optional parameter: Include 24-hour change percentage. Default:
     *         false
     * @param  includeLastUpdatedAt  Optional parameter: Include last updated price time as a UNIX
     *         timestamp. Default: false
     * @param  precision  Optional parameter: Decimal places for currency price value
     * @return    Returns the Map of String, SimplePrice wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Map<String, SimplePrice>> simplePrice(
            final String vsCurrencies,
            final String ids,
            final String names,
            final String symbols,
            final IncludeTokens includeTokens,
            final Boolean includeMarketCap,
            final Boolean include24HrVol,
            final Boolean include24HrChange,
            final Boolean includeLastUpdatedAt,
            final Precision precision) throws ApiException, IOException {
        return prepareSimplePriceRequest(vsCurrencies, ids, names, symbols, includeTokens,
                includeMarketCap, include24HrVol, include24HrChange, includeLastUpdatedAt,
                precision).execute();
    }

    /**
     * To query the prices of one or more coins by using their unique Coin API IDs, symbols, or
     * names.
     * @param  vsCurrencies  Required parameter: Target currency of coins, comma-separated if
     *         querying more than 1 currency. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies)
     * @param  ids  Optional parameter: Coins' IDs, comma-separated if querying more than 1 coin.
     *         *refers to [`/coins/list`](/reference/coins-list)
     * @param  names  Optional parameter: Coins' names, comma-separated if querying more than 1
     *         coin.
     * @param  symbols  Optional parameter: Coins' symbols, comma-separated if querying more than 1
     *         coin.
     * @param  includeTokens  Optional parameter: For `symbols` lookups, specify `all` to include
     *         all matching tokens. Default `top` returns top-ranked tokens by market cap or volume.
     * @param  includeMarketCap  Optional parameter: Include market capitalization. Default: false
     * @param  include24HrVol  Optional parameter: Include 24-hour trading volume. Default: false
     * @param  include24HrChange  Optional parameter: Include 24-hour change percentage. Default:
     *         false
     * @param  includeLastUpdatedAt  Optional parameter: Include last updated price time as a UNIX
     *         timestamp. Default: false
     * @param  precision  Optional parameter: Decimal places for currency price value
     * @return    Returns the Map of String, SimplePrice wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Map<String, SimplePrice>>> simplePriceAsync(
            final String vsCurrencies,
            final String ids,
            final String names,
            final String symbols,
            final IncludeTokens includeTokens,
            final Boolean includeMarketCap,
            final Boolean include24HrVol,
            final Boolean include24HrChange,
            final Boolean includeLastUpdatedAt,
            final Precision precision) {
        try {
            return prepareSimplePriceRequest(vsCurrencies, ids, names, symbols, includeTokens,
            includeMarketCap, include24HrVol, include24HrChange, includeLastUpdatedAt,
            precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for simplePrice.
     */
    private ApiCall<ApiResponse<Map<String, SimplePrice>>, ApiException> prepareSimplePriceRequest(
            final String vsCurrencies,
            final String ids,
            final String names,
            final String symbols,
            final IncludeTokens includeTokens,
            final Boolean includeMarketCap,
            final Boolean include24HrVol,
            final Boolean include24HrChange,
            final Boolean includeLastUpdatedAt,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<Map<String, SimplePrice>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/simple/price")
                        .queryParam(param -> param.key("vs_currencies")
                                .value(vsCurrencies))
                        .queryParam(param -> param.key("ids")
                                .value((ids != null) ? ids : "bitcoin").isRequired(false))
                        .queryParam(param -> param.key("names")
                                .value((names != null) ? names : "Bitcoin").isRequired(false))
                        .queryParam(param -> param.key("symbols")
                                .value((symbols != null) ? symbols : "btc").isRequired(false))
                        .queryParam(param -> param.key("include_tokens")
                                .value((includeTokens != null) ? includeTokens.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_market_cap")
                                .value(includeMarketCap).isRequired(false))
                        .queryParam(param -> param.key("include_24hr_vol")
                                .value(include24HrVol).isRequired(false))
                        .queryParam(param -> param.key("include_24hr_change")
                                .value(include24HrChange).isRequired(false))
                        .queryParam(param -> param.key("include_last_updated_at")
                                .value(includeLastUpdatedAt).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response,
                                        new TypeReference<Map<String, SimplePrice>>(){}))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To search for coins, categories and markets listed on CoinGecko.
     * @param  query  Required parameter: Search query
     * @return    Returns the Search wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Search> searchData(
            final String query) throws ApiException, IOException {
        return prepareSearchDataRequest(query).execute();
    }

    /**
     * To search for coins, categories and markets listed on CoinGecko.
     * @param  query  Required parameter: Search query
     * @return    Returns the Search wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Search>> searchDataAsync(
            final String query) {
        try {
            return prepareSearchDataRequest(query).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for searchData.
     */
    private ApiCall<ApiResponse<Search>, ApiException> prepareSearchDataRequest(
            final String query) {
        return new ApiCall.Builder<ApiResponse<Search>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/search")
                        .queryParam(param -> param.key("query")
                                .value(query))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Search.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported currencies on CoinGecko.
     * @return    Returns the List of String wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<String>> simpleSupportedCurrencies() throws ApiException, IOException {
        return prepareSimpleSupportedCurrenciesRequest().execute();
    }

    /**
     * To query all the supported currencies on CoinGecko.
     * @return    Returns the List of String wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<String>>> simpleSupportedCurrenciesAsync() {
        try {
            return prepareSimpleSupportedCurrenciesRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for simpleSupportedCurrencies.
     */
    private ApiCall<ApiResponse<List<String>>, ApiException> prepareSimpleSupportedCurrenciesRequest() {
        return new ApiCall.Builder<ApiResponse<List<String>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/simple/supported_vs_currencies")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        String[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query one or more token prices by using their token contract addresses.
     * @param  id  Required parameter: Asset platform's ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list)
     * @param  contractAddresses  Required parameter: Token contract addresses, comma-separated if
     *         querying more than 1 token
     * @param  vsCurrencies  Required parameter: Target currency of coins, comma-separated if
     *         querying more than 1 currency. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies)
     * @param  includeMarketCap  Optional parameter: Include market capitalization. Default: false
     * @param  include24HrVol  Optional parameter: Include 24-hour trading volume. Default: false
     * @param  include24HrChange  Optional parameter: Include 24-hour change percentage. Default:
     *         false
     * @param  includeLastUpdatedAt  Optional parameter: Include last updated price time as a UNIX
     *         timestamp. Default: false
     * @param  precision  Optional parameter: Decimal places for currency price value
     * @return    Returns the Map of String, SimplePrice wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Map<String, SimplePrice>> simpleTokenPrice(
            final String id,
            final String contractAddresses,
            final String vsCurrencies,
            final Boolean includeMarketCap,
            final Boolean include24HrVol,
            final Boolean include24HrChange,
            final Boolean includeLastUpdatedAt,
            final Precision precision) throws ApiException, IOException {
        return prepareSimpleTokenPriceRequest(id, contractAddresses, vsCurrencies, includeMarketCap,
                include24HrVol, include24HrChange, includeLastUpdatedAt, precision).execute();
    }

    /**
     * To query one or more token prices by using their token contract addresses.
     * @param  id  Required parameter: Asset platform's ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list)
     * @param  contractAddresses  Required parameter: Token contract addresses, comma-separated if
     *         querying more than 1 token
     * @param  vsCurrencies  Required parameter: Target currency of coins, comma-separated if
     *         querying more than 1 currency. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies)
     * @param  includeMarketCap  Optional parameter: Include market capitalization. Default: false
     * @param  include24HrVol  Optional parameter: Include 24-hour trading volume. Default: false
     * @param  include24HrChange  Optional parameter: Include 24-hour change percentage. Default:
     *         false
     * @param  includeLastUpdatedAt  Optional parameter: Include last updated price time as a UNIX
     *         timestamp. Default: false
     * @param  precision  Optional parameter: Decimal places for currency price value
     * @return    Returns the Map of String, SimplePrice wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Map<String, SimplePrice>>> simpleTokenPriceAsync(
            final String id,
            final String contractAddresses,
            final String vsCurrencies,
            final Boolean includeMarketCap,
            final Boolean include24HrVol,
            final Boolean include24HrChange,
            final Boolean includeLastUpdatedAt,
            final Precision precision) {
        try {
            return prepareSimpleTokenPriceRequest(id, contractAddresses, vsCurrencies, includeMarketCap,
            include24HrVol, include24HrChange, includeLastUpdatedAt, precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for simpleTokenPrice.
     */
    private ApiCall<ApiResponse<Map<String, SimplePrice>>, ApiException> prepareSimpleTokenPriceRequest(
            final String id,
            final String contractAddresses,
            final String vsCurrencies,
            final Boolean includeMarketCap,
            final Boolean include24HrVol,
            final Boolean include24HrChange,
            final Boolean includeLastUpdatedAt,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<Map<String, SimplePrice>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/simple/token_price/{id}")
                        .queryParam(param -> param.key("contract_addresses")
                                .value(contractAddresses))
                        .queryParam(param -> param.key("vs_currencies")
                                .value(vsCurrencies))
                        .queryParam(param -> param.key("include_market_cap")
                                .value(includeMarketCap).isRequired(false))
                        .queryParam(param -> param.key("include_24hr_vol")
                                .value(include24HrVol).isRequired(false))
                        .queryParam(param -> param.key("include_24hr_change")
                                .value(include24HrChange).isRequired(false))
                        .queryParam(param -> param.key("include_last_updated_at")
                                .value(includeLastUpdatedAt).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response,
                                        new TypeReference<Map<String, SimplePrice>>(){}))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported coins on CoinGecko with coin ID, name and symbol.
     * @param  includePlatform  Optional parameter: Include platform and token's contract addresses.
     *         Default: false
     * @param  status  Optional parameter: Filter by status of coins. Default: active
     * @return    Returns the List of CoinsList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<CoinsList>> coinsList(
            final Boolean includePlatform,
            final Status status) throws ApiException, IOException {
        return prepareCoinsListRequest(includePlatform, status).execute();
    }

    /**
     * To query all the supported coins on CoinGecko with coin ID, name and symbol.
     * @param  includePlatform  Optional parameter: Include platform and token's contract addresses.
     *         Default: false
     * @param  status  Optional parameter: Filter by status of coins. Default: active
     * @return    Returns the List of CoinsList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<CoinsList>>> coinsListAsync(
            final Boolean includePlatform,
            final Status status) {
        try {
            return prepareCoinsListRequest(includePlatform, status).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsList.
     */
    private ApiCall<ApiResponse<List<CoinsList>>, ApiException> prepareCoinsListRequest(
            final Boolean includePlatform,
            final Status status) {
        return new ApiCall.Builder<ApiResponse<List<CoinsList>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/list")
                        .queryParam(param -> param.key("include_platform")
                                .value(includePlatform).isRequired(false))
                        .queryParam(param -> param.key("status")
                                .value((status != null) ? status.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        CoinsList[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the metadata (image, websites, socials, description, contract address, etc.) and
     * market data (price, ATH, exchange tickers, etc.) of a coin based on a particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list)
     * @param  localization  Optional parameter: Include all localized languages in the response.
     *         Default: true
     * @param  tickers  Optional parameter: Include tickers data. Default: true
     * @param  marketData  Optional parameter: Include market data. Default: true
     * @param  communityData  Optional parameter: Include community data. Default: true
     * @param  developerData  Optional parameter: Include developer data. Default: true
     * @param  sparkline  Optional parameter: Include sparkline 7-day data. Default: false
     * @param  includeCategoriesDetails  Optional parameter: Include categories details. Default:
     *         false
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the CoinsId wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsId> coinsId(
            final String id,
            final Boolean localization,
            final Boolean tickers,
            final Boolean marketData,
            final Boolean communityData,
            final Boolean developerData,
            final Boolean sparkline,
            final Boolean includeCategoriesDetails,
            final DexPairFormat dexPairFormat) throws ApiException, IOException {
        return prepareCoinsIdRequest(id, localization, tickers, marketData, communityData,
                developerData, sparkline, includeCategoriesDetails, dexPairFormat).execute();
    }

    /**
     * To query all the metadata (image, websites, socials, description, contract address, etc.) and
     * market data (price, ATH, exchange tickers, etc.) of a coin based on a particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list)
     * @param  localization  Optional parameter: Include all localized languages in the response.
     *         Default: true
     * @param  tickers  Optional parameter: Include tickers data. Default: true
     * @param  marketData  Optional parameter: Include market data. Default: true
     * @param  communityData  Optional parameter: Include community data. Default: true
     * @param  developerData  Optional parameter: Include developer data. Default: true
     * @param  sparkline  Optional parameter: Include sparkline 7-day data. Default: false
     * @param  includeCategoriesDetails  Optional parameter: Include categories details. Default:
     *         false
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the CoinsId wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsId>> coinsIdAsync(
            final String id,
            final Boolean localization,
            final Boolean tickers,
            final Boolean marketData,
            final Boolean communityData,
            final Boolean developerData,
            final Boolean sparkline,
            final Boolean includeCategoriesDetails,
            final DexPairFormat dexPairFormat) {
        try {
            return prepareCoinsIdRequest(id, localization, tickers, marketData, communityData,
            developerData, sparkline, includeCategoriesDetails, dexPairFormat).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsId.
     */
    private ApiCall<ApiResponse<CoinsId>, ApiException> prepareCoinsIdRequest(
            final String id,
            final Boolean localization,
            final Boolean tickers,
            final Boolean marketData,
            final Boolean communityData,
            final Boolean developerData,
            final Boolean sparkline,
            final Boolean includeCategoriesDetails,
            final DexPairFormat dexPairFormat) {
        return new ApiCall.Builder<ApiResponse<CoinsId>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}")
                        .queryParam(param -> param.key("localization")
                                .value(localization).isRequired(false))
                        .queryParam(param -> param.key("tickers")
                                .value(tickers).isRequired(false))
                        .queryParam(param -> param.key("market_data")
                                .value(marketData).isRequired(false))
                        .queryParam(param -> param.key("community_data")
                                .value(communityData).isRequired(false))
                        .queryParam(param -> param.key("developer_data")
                                .value(developerData).isRequired(false))
                        .queryParam(param -> param.key("sparkline")
                                .value(sparkline).isRequired(false))
                        .queryParam(param -> param.key("include_categories_details")
                                .value(includeCategoriesDetails).isRequired(false))
                        .queryParam(param -> param.key("dex_pair_format")
                                .value((dexPairFormat != null) ? dexPairFormat.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsId.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported coins with price, market cap, volume and market related data.
     * @param  vsCurrency  Required parameter: Target currency of coins and market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies)
     * @param  ids  Optional parameter: Coins' IDs, comma-separated if querying more than 1 coin.
     *         *refers to [`/coins/list`](/reference/coins-list)
     * @param  names  Optional parameter: Coins' names, comma-separated if querying more than 1
     *         coin.
     * @param  symbols  Optional parameter: Coins' symbols, comma-separated if querying more than 1
     *         coin.
     * @param  includeTokens  Optional parameter: For `symbols` lookups, specify `all` to include
     *         all matching tokens. Default `top` returns top-ranked tokens by market cap or volume.
     * @param  category  Optional parameter: Filter based on coins' category. *refers to
     *         [`/coins/categories/list`](/reference/coins-categories-list)
     * @param  order  Optional parameter: Sort result by field. Default: market_cap_desc
     * @param  perPage  Optional parameter: Total results per page. Default: 100 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default: 1
     * @param  sparkline  Optional parameter: Include sparkline 7-day data. Default: false
     * @param  priceChangePercentage  Optional parameter: Include price change percentage timeframe,
     *         comma-separated if querying more than 1 timeframe. Valid values: `1h`, `24h`, `7d`,
     *         `14d`, `30d`, `200d`, `1y`
     * @param  locale  Optional parameter: Language background. Default: en
     * @param  precision  Optional parameter: Decimal places for currency price value
     * @param  includeRehypothecated  Optional parameter: Include rehypothecated tokens in results.
     *         When true, returns `market_cap_rank_with_rehypothecated` field. Default: false
     * @return    Returns the List of CoinsMarket wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<CoinsMarket>> coinsMarkets(
            final String vsCurrency,
            final String ids,
            final String names,
            final String symbols,
            final IncludeTokens includeTokens,
            final String category,
            final Order order,
            final Integer perPage,
            final Integer page,
            final Boolean sparkline,
            final String priceChangePercentage,
            final Locale locale,
            final Precision precision,
            final Boolean includeRehypothecated) throws ApiException, IOException {
        return prepareCoinsMarketsRequest(vsCurrency, ids, names, symbols, includeTokens, category,
                order, perPage, page, sparkline, priceChangePercentage, locale, precision,
                includeRehypothecated).execute();
    }

    /**
     * To query all the supported coins with price, market cap, volume and market related data.
     * @param  vsCurrency  Required parameter: Target currency of coins and market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies)
     * @param  ids  Optional parameter: Coins' IDs, comma-separated if querying more than 1 coin.
     *         *refers to [`/coins/list`](/reference/coins-list)
     * @param  names  Optional parameter: Coins' names, comma-separated if querying more than 1
     *         coin.
     * @param  symbols  Optional parameter: Coins' symbols, comma-separated if querying more than 1
     *         coin.
     * @param  includeTokens  Optional parameter: For `symbols` lookups, specify `all` to include
     *         all matching tokens. Default `top` returns top-ranked tokens by market cap or volume.
     * @param  category  Optional parameter: Filter based on coins' category. *refers to
     *         [`/coins/categories/list`](/reference/coins-categories-list)
     * @param  order  Optional parameter: Sort result by field. Default: market_cap_desc
     * @param  perPage  Optional parameter: Total results per page. Default: 100 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default: 1
     * @param  sparkline  Optional parameter: Include sparkline 7-day data. Default: false
     * @param  priceChangePercentage  Optional parameter: Include price change percentage timeframe,
     *         comma-separated if querying more than 1 timeframe. Valid values: `1h`, `24h`, `7d`,
     *         `14d`, `30d`, `200d`, `1y`
     * @param  locale  Optional parameter: Language background. Default: en
     * @param  precision  Optional parameter: Decimal places for currency price value
     * @param  includeRehypothecated  Optional parameter: Include rehypothecated tokens in results.
     *         When true, returns `market_cap_rank_with_rehypothecated` field. Default: false
     * @return    Returns the List of CoinsMarket wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<CoinsMarket>>> coinsMarketsAsync(
            final String vsCurrency,
            final String ids,
            final String names,
            final String symbols,
            final IncludeTokens includeTokens,
            final String category,
            final Order order,
            final Integer perPage,
            final Integer page,
            final Boolean sparkline,
            final String priceChangePercentage,
            final Locale locale,
            final Precision precision,
            final Boolean includeRehypothecated) {
        try {
            return prepareCoinsMarketsRequest(vsCurrency, ids, names, symbols, includeTokens, category,
            order, perPage, page, sparkline, priceChangePercentage, locale, precision,
            includeRehypothecated).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsMarkets.
     */
    private ApiCall<ApiResponse<List<CoinsMarket>>, ApiException> prepareCoinsMarketsRequest(
            final String vsCurrency,
            final String ids,
            final String names,
            final String symbols,
            final IncludeTokens includeTokens,
            final String category,
            final Order order,
            final Integer perPage,
            final Integer page,
            final Boolean sparkline,
            final String priceChangePercentage,
            final Locale locale,
            final Precision precision,
            final Boolean includeRehypothecated) {
        return new ApiCall.Builder<ApiResponse<List<CoinsMarket>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/markets")
                        .queryParam(param -> param.key("vs_currency")
                                .value(vsCurrency))
                        .queryParam(param -> param.key("ids")
                                .value((ids != null) ? ids : "bitcoin").isRequired(false))
                        .queryParam(param -> param.key("names")
                                .value((names != null) ? names : "Bitcoin").isRequired(false))
                        .queryParam(param -> param.key("symbols")
                                .value((symbols != null) ? symbols : "btc").isRequired(false))
                        .queryParam(param -> param.key("include_tokens")
                                .value((includeTokens != null) ? includeTokens.value() : null).isRequired(false))
                        .queryParam(param -> param.key("category")
                                .value(category).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("sparkline")
                                .value(sparkline).isRequired(false))
                        .queryParam(param -> param.key("price_change_percentage")
                                .value(priceChangePercentage).isRequired(false))
                        .queryParam(param -> param.key("locale")
                                .value((locale != null) ? locale.value() : null).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_rehypothecated")
                                .value(includeRehypothecated).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        CoinsMarket[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the coin tickers on both centralized exchange (CEX) and decentralized exchange (DEX)
     * based on a particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list)
     * @param  exchangeIds  Optional parameter: Exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list)
     * @param  includeExchangeLogo  Optional parameter: Include exchange logo. Default: false
     * @param  page  Optional parameter: Page through results
     * @param  order  Optional parameter: Sort the order of responses. Default: trust_score_desc
     * @param  depth  Optional parameter: Include 2% orderbook depth, i.e. `cost_to_move_up_usd` and
     *         `cost_to_move_down_usd`. Default: false
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the CoinsIdTickers wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsIdTickers> coinsIdTickers(
            final String id,
            final String exchangeIds,
            final Boolean includeExchangeLogo,
            final Integer page,
            final Order1 order,
            final Boolean depth,
            final DexPairFormat dexPairFormat) throws ApiException, IOException {
        return prepareCoinsIdTickersRequest(id, exchangeIds, includeExchangeLogo, page, order,
                depth, dexPairFormat).execute();
    }

    /**
     * To query the coin tickers on both centralized exchange (CEX) and decentralized exchange (DEX)
     * based on a particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list)
     * @param  exchangeIds  Optional parameter: Exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list)
     * @param  includeExchangeLogo  Optional parameter: Include exchange logo. Default: false
     * @param  page  Optional parameter: Page through results
     * @param  order  Optional parameter: Sort the order of responses. Default: trust_score_desc
     * @param  depth  Optional parameter: Include 2% orderbook depth, i.e. `cost_to_move_up_usd` and
     *         `cost_to_move_down_usd`. Default: false
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the CoinsIdTickers wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsIdTickers>> coinsIdTickersAsync(
            final String id,
            final String exchangeIds,
            final Boolean includeExchangeLogo,
            final Integer page,
            final Order1 order,
            final Boolean depth,
            final DexPairFormat dexPairFormat) {
        try {
            return prepareCoinsIdTickersRequest(id, exchangeIds, includeExchangeLogo, page, order, depth,
            dexPairFormat).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsIdTickers.
     */
    private ApiCall<ApiResponse<CoinsIdTickers>, ApiException> prepareCoinsIdTickersRequest(
            final String id,
            final String exchangeIds,
            final Boolean includeExchangeLogo,
            final Integer page,
            final Order1 order,
            final Boolean depth,
            final DexPairFormat dexPairFormat) {
        return new ApiCall.Builder<ApiResponse<CoinsIdTickers>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/tickers")
                        .queryParam(param -> param.key("exchange_ids")
                                .value(exchangeIds).isRequired(false))
                        .queryParam(param -> param.key("include_exchange_logo")
                                .value(includeExchangeLogo).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("depth")
                                .value(depth).isRequired(false))
                        .queryParam(param -> param.key("dex_pair_format")
                                .value((dexPairFormat != null) ? dexPairFormat.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsIdTickers.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the historical data (price, market cap, 24hrs volume, etc.) at a given date for a
     * coin based on a particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  date  Required parameter: The date of data snapshot. Format: `dd-mm-yyyy`
     * @param  localization  Optional parameter: Include all the localized languages in response.
     *         Default: true
     * @return    Returns the CoinsIdHistory wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsIdHistory> coinsIdHistory(
            final String id,
            final String date,
            final Boolean localization) throws ApiException, IOException {
        return prepareCoinsIdHistoryRequest(id, date, localization).execute();
    }

    /**
     * To query the historical data (price, market cap, 24hrs volume, etc.) at a given date for a
     * coin based on a particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  date  Required parameter: The date of data snapshot. Format: `dd-mm-yyyy`
     * @param  localization  Optional parameter: Include all the localized languages in response.
     *         Default: true
     * @return    Returns the CoinsIdHistory wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsIdHistory>> coinsIdHistoryAsync(
            final String id,
            final String date,
            final Boolean localization) {
        try {
            return prepareCoinsIdHistoryRequest(id, date, localization).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsIdHistory.
     */
    private ApiCall<ApiResponse<CoinsIdHistory>, ApiException> prepareCoinsIdHistoryRequest(
            final String id,
            final String date,
            final Boolean localization) {
        return new ApiCall.Builder<ApiResponse<CoinsIdHistory>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/history")
                        .queryParam(param -> param.key("date")
                                .value(date))
                        .queryParam(param -> param.key("localization")
                                .value(localization).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsIdHistory.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get the historical chart data of a coin including time in UNIX, price, market cap and
     * 24hrs volume based on particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  days  Required parameter: Data up to number of days ago. You may use any integer or
     *         `max` for number of days.
     * @param  interval  Optional parameter: Data interval, leave empty for auto granularity.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsMarketChart> coinsIdMarketChart(
            final String id,
            final String vsCurrency,
            final String days,
            final Interval interval,
            final Precision precision) throws ApiException, IOException {
        return prepareCoinsIdMarketChartRequest(id, vsCurrency, days, interval,
                precision).execute();
    }

    /**
     * To get the historical chart data of a coin including time in UNIX, price, market cap and
     * 24hrs volume based on particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  days  Required parameter: Data up to number of days ago. You may use any integer or
     *         `max` for number of days.
     * @param  interval  Optional parameter: Data interval, leave empty for auto granularity.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsMarketChart>> coinsIdMarketChartAsync(
            final String id,
            final String vsCurrency,
            final String days,
            final Interval interval,
            final Precision precision) {
        try {
            return prepareCoinsIdMarketChartRequest(id, vsCurrency, days, interval,
            precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsIdMarketChart.
     */
    private ApiCall<ApiResponse<CoinsMarketChart>, ApiException> prepareCoinsIdMarketChartRequest(
            final String id,
            final String vsCurrency,
            final String days,
            final Interval interval,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<CoinsMarketChart>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/market_chart")
                        .queryParam(param -> param.key("vs_currency")
                                .value(vsCurrency))
                        .queryParam(param -> param.key("days")
                                .value(days))
                        .queryParam(param -> param.key("interval")
                                .value((interval != null) ? interval.value() : null).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsMarketChart.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get the historical chart data of a coin within certain time range in UNIX along with
     * price, market cap and 24hrs volume based on particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  from  Required parameter: Starting date in UNIX timestamp.
     * @param  to  Required parameter: Ending date in UNIX timestamp.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsMarketChart> coinsIdMarketChartRange(
            final String id,
            final String vsCurrency,
            final int from,
            final int to,
            final Precision precision) throws ApiException, IOException {
        return prepareCoinsIdMarketChartRangeRequest(id, vsCurrency, from, to, precision).execute();
    }

    /**
     * To get the historical chart data of a coin within certain time range in UNIX along with
     * price, market cap and 24hrs volume based on particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  from  Required parameter: Starting date in UNIX timestamp.
     * @param  to  Required parameter: Ending date in UNIX timestamp.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsMarketChart>> coinsIdMarketChartRangeAsync(
            final String id,
            final String vsCurrency,
            final int from,
            final int to,
            final Precision precision) {
        try {
            return prepareCoinsIdMarketChartRangeRequest(id, vsCurrency, from, to,
            precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsIdMarketChartRange.
     */
    private ApiCall<ApiResponse<CoinsMarketChart>, ApiException> prepareCoinsIdMarketChartRangeRequest(
            final String id,
            final String vsCurrency,
            final int from,
            final int to,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<CoinsMarketChart>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/market_chart/range")
                        .queryParam(param -> param.key("vs_currency")
                                .value(vsCurrency))
                        .queryParam(param -> param.key("from")
                                .value(from).isRequired(false))
                        .queryParam(param -> param.key("to")
                                .value(to).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsMarketChart.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get the OHLC chart (Open, High, Low, Close) of a coin based on particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  vsCurrency  Required parameter: Target currency of price data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  days  Required parameter: Data up to number of days ago.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the List of Double wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<List<Double>>> coinsIdOhlc(
            final String id,
            final String vsCurrency,
            final Days days,
            final Precision precision) throws ApiException, IOException {
        return prepareCoinsIdOhlcRequest(id, vsCurrency, days, precision).execute();
    }

    /**
     * To get the OHLC chart (Open, High, Low, Close) of a coin based on particular coin ID.
     * @param  id  Required parameter: Coin ID. *refers to [`/coins/list`](/reference/coins-list).
     * @param  vsCurrency  Required parameter: Target currency of price data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  days  Required parameter: Data up to number of days ago.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the List of Double wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<List<Double>>>> coinsIdOhlcAsync(
            final String id,
            final String vsCurrency,
            final Days days,
            final Precision precision) {
        try {
            return prepareCoinsIdOhlcRequest(id, vsCurrency, days, precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsIdOhlc.
     */
    private ApiCall<ApiResponse<List<List<Double>>>, ApiException> prepareCoinsIdOhlcRequest(
            final String id,
            final String vsCurrency,
            final Days days,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<List<List<Double>>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/ohlc")
                        .queryParam(param -> param.key("vs_currency")
                                .value(vsCurrency))
                        .queryParam(param -> param.key("days")
                                .value((days != null) ? days.value() : "1"))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response,
                                        new TypeReference<List<List<Double>>>(){}))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the metadata (image, websites, socials, description, contract address, etc.) and
     * market data (price, ATH, exchange tickers, etc.) of a coin based on an asset platform and a
     * particular token contract address.
     * @param  id  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: The contract address of token.
     * @return    Returns the CoinsContractAddress wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsContractAddress> coinsContractAddress(
            final String id,
            final String contractAddress) throws ApiException, IOException {
        return prepareCoinsContractAddressRequest(id, contractAddress).execute();
    }

    /**
     * To query all the metadata (image, websites, socials, description, contract address, etc.) and
     * market data (price, ATH, exchange tickers, etc.) of a coin based on an asset platform and a
     * particular token contract address.
     * @param  id  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: The contract address of token.
     * @return    Returns the CoinsContractAddress wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsContractAddress>> coinsContractAddressAsync(
            final String id,
            final String contractAddress) {
        try {
            return prepareCoinsContractAddressRequest(id, contractAddress).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsContractAddress.
     */
    private ApiCall<ApiResponse<CoinsContractAddress>, ApiException> prepareCoinsContractAddressRequest(
            final String id,
            final String contractAddress) {
        return new ApiCall.Builder<ApiResponse<CoinsContractAddress>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/contract/{contract_address}")
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("contract_address").value(contractAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsContractAddress.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get the historical chart data including time in UNIX, price, market cap and 24hrs volume
     * based on asset platform and particular token contract address.
     * @param  id  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: The contract address of token.
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  days  Required parameter: Data up to number of days ago. You may use any integer or
     *         `max` for number of days.
     * @param  interval  Optional parameter: Data interval, leave empty for auto granularity.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsMarketChart> contractAddressMarketChart(
            final String id,
            final String contractAddress,
            final String vsCurrency,
            final String days,
            final Interval interval,
            final Precision precision) throws ApiException, IOException {
        return prepareContractAddressMarketChartRequest(id, contractAddress, vsCurrency, days,
                interval, precision).execute();
    }

    /**
     * To get the historical chart data including time in UNIX, price, market cap and 24hrs volume
     * based on asset platform and particular token contract address.
     * @param  id  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: The contract address of token.
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  days  Required parameter: Data up to number of days ago. You may use any integer or
     *         `max` for number of days.
     * @param  interval  Optional parameter: Data interval, leave empty for auto granularity.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsMarketChart>> contractAddressMarketChartAsync(
            final String id,
            final String contractAddress,
            final String vsCurrency,
            final String days,
            final Interval interval,
            final Precision precision) {
        try {
            return prepareContractAddressMarketChartRequest(id, contractAddress, vsCurrency, days, interval,
            precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for contractAddressMarketChart.
     */
    private ApiCall<ApiResponse<CoinsMarketChart>, ApiException> prepareContractAddressMarketChartRequest(
            final String id,
            final String contractAddress,
            final String vsCurrency,
            final String days,
            final Interval interval,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<CoinsMarketChart>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/contract/{contract_address}/market_chart")
                        .queryParam(param -> param.key("vs_currency")
                                .value(vsCurrency))
                        .queryParam(param -> param.key("days")
                                .value(days))
                        .queryParam(param -> param.key("interval")
                                .value((interval != null) ? interval.value() : null).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("contract_address").value(contractAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsMarketChart.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get the historical chart data within certain time range in UNIX along with price, market
     * cap and 24hrs volume based on asset platform and particular token contract address.
     * @param  id  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: The contract address of token.
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  from  Required parameter: Starting date in UNIX timestamp.
     * @param  to  Required parameter: Ending date in UNIX timestamp.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsMarketChart> contractAddressMarketChartRange(
            final String id,
            final String contractAddress,
            final String vsCurrency,
            final int from,
            final int to,
            final Precision precision) throws ApiException, IOException {
        return prepareContractAddressMarketChartRangeRequest(id, contractAddress, vsCurrency, from,
                to, precision).execute();
    }

    /**
     * To get the historical chart data within certain time range in UNIX along with price, market
     * cap and 24hrs volume based on asset platform and particular token contract address.
     * @param  id  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: The contract address of token.
     * @param  vsCurrency  Required parameter: Target currency of market data. *refers to
     *         [`/simple/supported_vs_currencies`](/reference/simple-supported-currencies).
     * @param  from  Required parameter: Starting date in UNIX timestamp.
     * @param  to  Required parameter: Ending date in UNIX timestamp.
     * @param  precision  Optional parameter: Decimal place for currency price value.
     * @return    Returns the CoinsMarketChart wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsMarketChart>> contractAddressMarketChartRangeAsync(
            final String id,
            final String contractAddress,
            final String vsCurrency,
            final int from,
            final int to,
            final Precision precision) {
        try {
            return prepareContractAddressMarketChartRangeRequest(id, contractAddress, vsCurrency, from, to,
            precision).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for contractAddressMarketChartRange.
     */
    private ApiCall<ApiResponse<CoinsMarketChart>, ApiException> prepareContractAddressMarketChartRangeRequest(
            final String id,
            final String contractAddress,
            final String vsCurrency,
            final int from,
            final int to,
            final Precision precision) {
        return new ApiCall.Builder<ApiResponse<CoinsMarketChart>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/{id}/contract/{contract_address}/market_chart/range")
                        .queryParam(param -> param.key("vs_currency")
                                .value(vsCurrency))
                        .queryParam(param -> param.key("from")
                                .value(from).isRequired(false))
                        .queryParam(param -> param.key("to")
                                .value(to).isRequired(false))
                        .queryParam(param -> param.key("precision")
                                .value((precision != null) ? precision.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("contract_address").value(contractAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsMarketChart.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported asset platforms (blockchain networks) on CoinGecko.
     * @param  filter  Optional parameter: Apply relevant filters to results.
     * @return    Returns the List of AssetPlatform wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<AssetPlatform>> assetPlatformsList(
            final Filter filter) throws ApiException, IOException {
        return prepareAssetPlatformsListRequest(filter).execute();
    }

    /**
     * To query all the supported asset platforms (blockchain networks) on CoinGecko.
     * @param  filter  Optional parameter: Apply relevant filters to results.
     * @return    Returns the List of AssetPlatform wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<AssetPlatform>>> assetPlatformsListAsync(
            final Filter filter) {
        try {
            return prepareAssetPlatformsListRequest(filter).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for assetPlatformsList.
     */
    private ApiCall<ApiResponse<List<AssetPlatform>>, ApiException> prepareAssetPlatformsListRequest(
            final Filter filter) {
        return new ApiCall.Builder<ApiResponse<List<AssetPlatform>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/asset_platforms")
                        .queryParam(param -> param.key("filter")
                                .value((filter != null) ? filter.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        AssetPlatform[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get full list of tokens of a blockchain network (asset platform) that is supported by
     * [Ethereum token list standard](https://tokenlists.org/).
     * @param  assetPlatformId  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @return    Returns the TokenLists wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TokenLists> tokenLists(
            final String assetPlatformId) throws ApiException, IOException {
        return prepareTokenListsRequest(assetPlatformId).execute();
    }

    /**
     * To get full list of tokens of a blockchain network (asset platform) that is supported by
     * [Ethereum token list standard](https://tokenlists.org/).
     * @param  assetPlatformId  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @return    Returns the TokenLists wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TokenLists>> tokenListsAsync(
            final String assetPlatformId) {
        try {
            return prepareTokenListsRequest(assetPlatformId).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for tokenLists.
     */
    private ApiCall<ApiResponse<TokenLists>, ApiException> prepareTokenListsRequest(
            final String assetPlatformId) {
        return new ApiCall.Builder<ApiResponse<TokenLists>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/token_lists/{asset_platform_id}/all.json")
                        .templateParam(param -> param.key("asset_platform_id").value(assetPlatformId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TokenLists.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported coins categories on CoinGecko.
     * @return    Returns the List of CategoriesList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<CategoriesList>> coinsCategoriesList() throws ApiException, IOException {
        return prepareCoinsCategoriesListRequest().execute();
    }

    /**
     * To query all the supported coins categories on CoinGecko.
     * @return    Returns the List of CategoriesList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<CategoriesList>>> coinsCategoriesListAsync() {
        try {
            return prepareCoinsCategoriesListRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsCategoriesList.
     */
    private ApiCall<ApiResponse<List<CategoriesList>>, ApiException> prepareCoinsCategoriesListRequest() {
        return new ApiCall.Builder<ApiResponse<List<CategoriesList>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/categories/list")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        CategoriesList[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the coins categories with market data (market cap, volume, etc.) on CoinGecko.
     * @param  order  Optional parameter: Sort results by field. Default: `market_cap_desc`
     * @return    Returns the List of Category1 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<Category1>> coinsCategories(
            final Order2 order) throws ApiException, IOException {
        return prepareCoinsCategoriesRequest(order).execute();
    }

    /**
     * To query all the coins categories with market data (market cap, volume, etc.) on CoinGecko.
     * @param  order  Optional parameter: Sort results by field. Default: `market_cap_desc`
     * @return    Returns the List of Category1 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<Category1>>> coinsCategoriesAsync(
            final Order2 order) {
        try {
            return prepareCoinsCategoriesRequest(order).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for coinsCategories.
     */
    private ApiCall<ApiResponse<List<Category1>>, ApiException> prepareCoinsCategoriesRequest(
            final Order2 order) {
        return new ApiCall.Builder<ApiResponse<List<Category1>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/coins/categories")
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        Category1[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported exchanges with exchanges' data (ID, name, country, etc.) that have
     * active trading volumes on CoinGecko.
     * @param  perPage  Optional parameter: Total results per page. Default: 100. Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default: 1
     * @return    Returns the List of Exchange1 wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<Exchange1>> exchanges(
            final Double perPage,
            final Double page) throws ApiException, IOException {
        return prepareExchangesRequest(perPage, page).execute();
    }

    /**
     * To query all the supported exchanges with exchanges' data (ID, name, country, etc.) that have
     * active trading volumes on CoinGecko.
     * @param  perPage  Optional parameter: Total results per page. Default: 100. Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default: 1
     * @return    Returns the List of Exchange1 wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<Exchange1>>> exchangesAsync(
            final Double perPage,
            final Double page) {
        try {
            return prepareExchangesRequest(perPage, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exchanges.
     */
    private ApiCall<ApiResponse<List<Exchange1>>, ApiException> prepareExchangesRequest(
            final Double perPage,
            final Double page) {
        return new ApiCall.Builder<ApiResponse<List<Exchange1>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/exchanges")
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        Exchange1[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported exchanges with ID and name.
     * @param  status  Optional parameter: Filter by status of exchanges. Default: `active`
     * @return    Returns the List of ExchangesList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<ExchangesList>> exchangesList(
            final Status status) throws ApiException, IOException {
        return prepareExchangesListRequest(status).execute();
    }

    /**
     * To query all the supported exchanges with ID and name.
     * @param  status  Optional parameter: Filter by status of exchanges. Default: `active`
     * @return    Returns the List of ExchangesList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<ExchangesList>>> exchangesListAsync(
            final Status status) {
        try {
            return prepareExchangesListRequest(status).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exchangesList.
     */
    private ApiCall<ApiResponse<List<ExchangesList>>, ApiException> prepareExchangesListRequest(
            final Status status) {
        return new ApiCall.Builder<ApiResponse<List<ExchangesList>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/exchanges/list")
                        .queryParam(param -> param.key("status")
                                .value((status != null) ? status.value() : null).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        ExchangesList[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query exchange's data (name, year established, country, etc.), exchange volume in BTC and
     * top 100 tickers based on exchange's ID.
     * @param  id  Required parameter: Exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list).
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the ExchangesId wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ExchangesId> exchangesId(
            final String id,
            final DexPairFormat dexPairFormat) throws ApiException, IOException {
        return prepareExchangesIdRequest(id, dexPairFormat).execute();
    }

    /**
     * To query exchange's data (name, year established, country, etc.), exchange volume in BTC and
     * top 100 tickers based on exchange's ID.
     * @param  id  Required parameter: Exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list).
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the ExchangesId wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ExchangesId>> exchangesIdAsync(
            final String id,
            final DexPairFormat dexPairFormat) {
        try {
            return prepareExchangesIdRequest(id, dexPairFormat).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exchangesId.
     */
    private ApiCall<ApiResponse<ExchangesId>, ApiException> prepareExchangesIdRequest(
            final String id,
            final DexPairFormat dexPairFormat) {
        return new ApiCall.Builder<ApiResponse<ExchangesId>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/exchanges/{id}")
                        .queryParam(param -> param.key("dex_pair_format")
                                .value((dexPairFormat != null) ? dexPairFormat.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ExchangesId.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query exchange's tickers based on exchange's ID.
     * @param  id  Required parameter: Exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list).
     * @param  coinIds  Optional parameter: Filter tickers by coin IDs, comma-separated if querying
     *         more than 1 coin. *refers to [`/coins/list`](/reference/coins-list).
     * @param  includeExchangeLogo  Optional parameter: Include exchange logo. Default: false
     * @param  page  Optional parameter: Page through results.
     * @param  depth  Optional parameter: Include 2% orderbook depth (cost_to_move_up_usd and
     *         cost_to_move_down_usd). Default: false
     * @param  order  Optional parameter: Sort the order of responses. Default: `trust_score_desc`
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the CoinsIdTickers wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<CoinsIdTickers> exchangesIdTickers(
            final String id,
            final String coinIds,
            final Boolean includeExchangeLogo,
            final Double page,
            final Boolean depth,
            final Order3 order,
            final DexPairFormat dexPairFormat) throws ApiException, IOException {
        return prepareExchangesIdTickersRequest(id, coinIds, includeExchangeLogo, page, depth,
                order, dexPairFormat).execute();
    }

    /**
     * To query exchange's tickers based on exchange's ID.
     * @param  id  Required parameter: Exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list).
     * @param  coinIds  Optional parameter: Filter tickers by coin IDs, comma-separated if querying
     *         more than 1 coin. *refers to [`/coins/list`](/reference/coins-list).
     * @param  includeExchangeLogo  Optional parameter: Include exchange logo. Default: false
     * @param  page  Optional parameter: Page through results.
     * @param  depth  Optional parameter: Include 2% orderbook depth (cost_to_move_up_usd and
     *         cost_to_move_down_usd). Default: false
     * @param  order  Optional parameter: Sort the order of responses. Default: `trust_score_desc`
     * @param  dexPairFormat  Optional parameter: Set to `symbol` to display DEX pair base and
     *         target as symbols. Default: `contract_address`
     * @return    Returns the CoinsIdTickers wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<CoinsIdTickers>> exchangesIdTickersAsync(
            final String id,
            final String coinIds,
            final Boolean includeExchangeLogo,
            final Double page,
            final Boolean depth,
            final Order3 order,
            final DexPairFormat dexPairFormat) {
        try {
            return prepareExchangesIdTickersRequest(id, coinIds, includeExchangeLogo, page, depth, order,
            dexPairFormat).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exchangesIdTickers.
     */
    private ApiCall<ApiResponse<CoinsIdTickers>, ApiException> prepareExchangesIdTickersRequest(
            final String id,
            final String coinIds,
            final Boolean includeExchangeLogo,
            final Double page,
            final Boolean depth,
            final Order3 order,
            final DexPairFormat dexPairFormat) {
        return new ApiCall.Builder<ApiResponse<CoinsIdTickers>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/exchanges/{id}/tickers")
                        .queryParam(param -> param.key("coin_ids")
                                .value(coinIds).isRequired(false))
                        .queryParam(param -> param.key("include_exchange_logo")
                                .value(includeExchangeLogo).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("depth")
                                .value(depth).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("dex_pair_format")
                                .value((dexPairFormat != null) ? dexPairFormat.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, CoinsIdTickers.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the historical volume chart data with time in UNIX and trading volume data in BTC
     * based on exchange's ID.
     * @param  id  Required parameter: Exchange ID or derivative exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list) or
     *         [`/derivatives/exchanges/list`](/reference/derivatives-exchanges-list).
     * @param  days  Required parameter: Data up to number of days ago.
     * @return    Returns the List of ExchangeVolumeChart wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<List<ExchangeVolumeChart>>> exchangesIdVolumeChart(
            final String id,
            final Days days) throws ApiException, IOException {
        return prepareExchangesIdVolumeChartRequest(id, days).execute();
    }

    /**
     * To query the historical volume chart data with time in UNIX and trading volume data in BTC
     * based on exchange's ID.
     * @param  id  Required parameter: Exchange ID or derivative exchange ID. *refers to
     *         [`/exchanges/list`](/reference/exchanges-list) or
     *         [`/derivatives/exchanges/list`](/reference/derivatives-exchanges-list).
     * @param  days  Required parameter: Data up to number of days ago.
     * @return    Returns the List of ExchangeVolumeChart wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<List<ExchangeVolumeChart>>>> exchangesIdVolumeChartAsync(
            final String id,
            final Days days) {
        try {
            return prepareExchangesIdVolumeChartRequest(id, days).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exchangesIdVolumeChart.
     */
    private ApiCall<ApiResponse<List<List<ExchangeVolumeChart>>>, ApiException> prepareExchangesIdVolumeChartRequest(
            final String id,
            final Days days) {
        return new ApiCall.Builder<ApiResponse<List<List<ExchangeVolumeChart>>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/exchanges/{id}/volume_chart")
                        .queryParam(param -> param.key("days")
                                .value((days != null) ? days.value() : "1"))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.DYNAMIC_API_RESPONSE)
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the tickers from derivatives exchanges on CoinGecko.
     * @return    Returns the List of DerivativesTicker wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DerivativesTicker>> derivativesTickers() throws ApiException, IOException {
        return prepareDerivativesTickersRequest().execute();
    }

    /**
     * To query all the tickers from derivatives exchanges on CoinGecko.
     * @return    Returns the List of DerivativesTicker wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DerivativesTicker>>> derivativesTickersAsync() {
        try {
            return prepareDerivativesTickersRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for derivativesTickers.
     */
    private ApiCall<ApiResponse<List<DerivativesTicker>>, ApiException> prepareDerivativesTickersRequest() {
        return new ApiCall.Builder<ApiResponse<List<DerivativesTicker>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/derivatives")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DerivativesTicker[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the derivatives exchanges with related data (ID, name, open interest, ...) on
     * CoinGecko.
     * @param  order  Optional parameter: Sort order of responses. Default: `open_interest_btc_desc`
     * @param  perPage  Optional parameter: Total results per page.
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the List of DerivativesExchange wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DerivativesExchange>> derivativesExchanges(
            final Order4 order,
            final Integer perPage,
            final Integer page) throws ApiException, IOException {
        return prepareDerivativesExchangesRequest(order, perPage, page).execute();
    }

    /**
     * To query all the derivatives exchanges with related data (ID, name, open interest, ...) on
     * CoinGecko.
     * @param  order  Optional parameter: Sort order of responses. Default: `open_interest_btc_desc`
     * @param  perPage  Optional parameter: Total results per page.
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the List of DerivativesExchange wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DerivativesExchange>>> derivativesExchangesAsync(
            final Order4 order,
            final Integer perPage,
            final Integer page) {
        try {
            return prepareDerivativesExchangesRequest(order, perPage, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for derivativesExchanges.
     */
    private ApiCall<ApiResponse<List<DerivativesExchange>>, ApiException> prepareDerivativesExchangesRequest(
            final Order4 order,
            final Integer perPage,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<List<DerivativesExchange>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/derivatives/exchanges")
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DerivativesExchange[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the derivatives exchange's related data (name, open interest, trade volume, ...)
     * based on the exchange's ID.
     * @param  id  Required parameter: Derivative exchange ID. *refers to
     *         [`/derivatives/exchanges/list`](/reference/derivatives-exchanges-list).
     * @param  includeTickers  Optional parameter: Include tickers data. Default: tickers data is
     *         not included.
     * @return    Returns the DerivativesExchangesId wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DerivativesExchangesId> derivativesExchangesId(
            final String id,
            final IncludeTickers includeTickers) throws ApiException, IOException {
        return prepareDerivativesExchangesIdRequest(id, includeTickers).execute();
    }

    /**
     * To query the derivatives exchange's related data (name, open interest, trade volume, ...)
     * based on the exchange's ID.
     * @param  id  Required parameter: Derivative exchange ID. *refers to
     *         [`/derivatives/exchanges/list`](/reference/derivatives-exchanges-list).
     * @param  includeTickers  Optional parameter: Include tickers data. Default: tickers data is
     *         not included.
     * @return    Returns the DerivativesExchangesId wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DerivativesExchangesId>> derivativesExchangesIdAsync(
            final String id,
            final IncludeTickers includeTickers) {
        try {
            return prepareDerivativesExchangesIdRequest(id, includeTickers).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for derivativesExchangesId.
     */
    private ApiCall<ApiResponse<DerivativesExchangesId>, ApiException> prepareDerivativesExchangesIdRequest(
            final String id,
            final IncludeTickers includeTickers) {
        return new ApiCall.Builder<ApiResponse<DerivativesExchangesId>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/derivatives/exchanges/{id}")
                        .queryParam(param -> param.key("include_tickers")
                                .value((includeTickers != null) ? includeTickers.value() : null).isRequired(false))
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DerivativesExchangesId.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported derivatives exchanges with ID and name on CoinGecko.
     * @return    Returns the List of DerivativesExchangesList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<DerivativesExchangesList>> derivativesExchangesList() throws ApiException, IOException {
        return prepareDerivativesExchangesListRequest().execute();
    }

    /**
     * To query all the supported derivatives exchanges with ID and name on CoinGecko.
     * @return    Returns the List of DerivativesExchangesList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<DerivativesExchangesList>>> derivativesExchangesListAsync() {
        try {
            return prepareDerivativesExchangesListRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for derivativesExchangesList.
     */
    private ApiCall<ApiResponse<List<DerivativesExchangesList>>, ApiException> prepareDerivativesExchangesListRequest() {
        return new ApiCall.Builder<ApiResponse<List<DerivativesExchangesList>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/derivatives/exchanges/list")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        DerivativesExchangesList[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported entities on CoinGecko with entity ID, name, symbol, and country.
     * @param  entityType  Optional parameter: Filter by entity type.
     * @param  perPage  Optional parameter: Total results per page. Default value: 100 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the List of EntitiesList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<EntitiesList>> entitiesList(
            final EntityType entityType,
            final Integer perPage,
            final Integer page) throws ApiException, IOException {
        return prepareEntitiesListRequest(entityType, perPage, page).execute();
    }

    /**
     * To query all the supported entities on CoinGecko with entity ID, name, symbol, and country.
     * @param  entityType  Optional parameter: Filter by entity type.
     * @param  perPage  Optional parameter: Total results per page. Default value: 100 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the List of EntitiesList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<EntitiesList>>> entitiesListAsync(
            final EntityType entityType,
            final Integer perPage,
            final Integer page) {
        try {
            return prepareEntitiesListRequest(entityType, perPage, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for entitiesList.
     */
    private ApiCall<ApiResponse<List<EntitiesList>>, ApiException> prepareEntitiesListRequest(
            final EntityType entityType,
            final Integer perPage,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<List<EntitiesList>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/entities/list")
                        .queryParam(param -> param.key("entity_type")
                                .value((entityType != null) ? entityType.value() : null).isRequired(false))
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        EntitiesList[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query public companies' and governments' cryptocurrency holdings by coin ID.
     * @param  entity  Required parameter: Public company or government entity.
     * @param  coinId  Required parameter: Coin ID. e.g. `bitcoin`, `ethereum`, `solana`,
     *         `binancecoin`
     * @param  perPage  Optional parameter: Total results per page. Default value: 250 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  order  Optional parameter: Sort order for results. Default: `total_holdings_usd_desc`
     * @return    Returns the PublicTreasury wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PublicTreasury> companiesPublicTreasury(
            final Entity entity,
            final String coinId,
            final Integer perPage,
            final Integer page,
            final Order5 order) throws ApiException, IOException {
        return prepareCompaniesPublicTreasuryRequest(entity, coinId, perPage, page,
                order).execute();
    }

    /**
     * To query public companies' and governments' cryptocurrency holdings by coin ID.
     * @param  entity  Required parameter: Public company or government entity.
     * @param  coinId  Required parameter: Coin ID. e.g. `bitcoin`, `ethereum`, `solana`,
     *         `binancecoin`
     * @param  perPage  Optional parameter: Total results per page. Default value: 250 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  order  Optional parameter: Sort order for results. Default: `total_holdings_usd_desc`
     * @return    Returns the PublicTreasury wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PublicTreasury>> companiesPublicTreasuryAsync(
            final Entity entity,
            final String coinId,
            final Integer perPage,
            final Integer page,
            final Order5 order) {
        try {
            return prepareCompaniesPublicTreasuryRequest(entity, coinId, perPage, page,
            order).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for companiesPublicTreasury.
     */
    private ApiCall<ApiResponse<PublicTreasury>, ApiException> prepareCompaniesPublicTreasuryRequest(
            final Entity entity,
            final String coinId,
            final Integer perPage,
            final Integer page,
            final Order5 order) {
        return new ApiCall.Builder<ApiResponse<PublicTreasury>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/{entity}/public_treasury/{coin_id}")
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .templateParam(param -> param.key("entity").value((entity != null) ? entity.value() : "companies")
                                .shouldEncode(true))
                        .templateParam(param -> param.key("coin_id").value(coinId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.DYNAMIC_API_RESPONSE)
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query public companies' and governments' cryptocurrency holdings by entity ID.
     * @param  entityId  Required parameter: Public company or government entity ID. *refers to
     *         [`/entities/list`](/reference/entities-list).
     * @param  holdingAmountChange  Optional parameter: Include holding amount change for specified
     *         timeframes, comma-separated if querying more than 1 timeframe. Valid values: `7d`,
     *         `14d`, `30d`, `90d`, `1y`, `ytd`
     * @param  holdingChangePercentage  Optional parameter: Include holding change percentage for
     *         specified timeframes, comma-separated if querying more than 1 timeframe. Valid
     *         values: `7d`, `14d`, `30d`, `90d`, `1y`, `ytd`
     * @return    Returns the PublicTreasuryEntity wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PublicTreasuryEntity> publicTreasuryEntity(
            final String entityId,
            final String holdingAmountChange,
            final String holdingChangePercentage) throws ApiException, IOException {
        return preparePublicTreasuryEntityRequest(entityId, holdingAmountChange,
                holdingChangePercentage).execute();
    }

    /**
     * To query public companies' and governments' cryptocurrency holdings by entity ID.
     * @param  entityId  Required parameter: Public company or government entity ID. *refers to
     *         [`/entities/list`](/reference/entities-list).
     * @param  holdingAmountChange  Optional parameter: Include holding amount change for specified
     *         timeframes, comma-separated if querying more than 1 timeframe. Valid values: `7d`,
     *         `14d`, `30d`, `90d`, `1y`, `ytd`
     * @param  holdingChangePercentage  Optional parameter: Include holding change percentage for
     *         specified timeframes, comma-separated if querying more than 1 timeframe. Valid
     *         values: `7d`, `14d`, `30d`, `90d`, `1y`, `ytd`
     * @return    Returns the PublicTreasuryEntity wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PublicTreasuryEntity>> publicTreasuryEntityAsync(
            final String entityId,
            final String holdingAmountChange,
            final String holdingChangePercentage) {
        try {
            return preparePublicTreasuryEntityRequest(entityId, holdingAmountChange,
            holdingChangePercentage).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for publicTreasuryEntity.
     */
    private ApiCall<ApiResponse<PublicTreasuryEntity>, ApiException> preparePublicTreasuryEntityRequest(
            final String entityId,
            final String holdingAmountChange,
            final String holdingChangePercentage) {
        return new ApiCall.Builder<ApiResponse<PublicTreasuryEntity>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/public_treasury/{entity_id}")
                        .queryParam(param -> param.key("holding_amount_change")
                                .value(holdingAmountChange).isRequired(false))
                        .queryParam(param -> param.key("holding_change_percentage")
                                .value(holdingChangePercentage).isRequired(false))
                        .templateParam(param -> param.key("entity_id").value(entityId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PublicTreasuryEntity.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query historical cryptocurrency holdings chart of public companies and governments by
     * entity ID and coin ID.
     * @param  entityId  Required parameter: Public company or government entity ID. *refers to
     *         [`/entities/list`](/reference/entities-list).
     * @param  coinId  Required parameter: Coin ID. e.g. `bitcoin`, `ethereum`, `solana`,
     *         `binancecoin`
     * @param  days  Required parameter: Data up to number of days ago. Valid values: `7`, `14`,
     *         `30`, `90`, `180`, `365`
     * @param  includeEmptyIntervals  Optional parameter: Include empty intervals with no
     *         transaction data. Default: `false`
     * @return    Returns the PublicTreasuryEntityChart wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PublicTreasuryEntityChart> publicTreasuryEntityChart(
            final String entityId,
            final String coinId,
            final String days,
            final Boolean includeEmptyIntervals) throws ApiException, IOException {
        return preparePublicTreasuryEntityChartRequest(entityId, coinId, days,
                includeEmptyIntervals).execute();
    }

    /**
     * To query historical cryptocurrency holdings chart of public companies and governments by
     * entity ID and coin ID.
     * @param  entityId  Required parameter: Public company or government entity ID. *refers to
     *         [`/entities/list`](/reference/entities-list).
     * @param  coinId  Required parameter: Coin ID. e.g. `bitcoin`, `ethereum`, `solana`,
     *         `binancecoin`
     * @param  days  Required parameter: Data up to number of days ago. Valid values: `7`, `14`,
     *         `30`, `90`, `180`, `365`
     * @param  includeEmptyIntervals  Optional parameter: Include empty intervals with no
     *         transaction data. Default: `false`
     * @return    Returns the PublicTreasuryEntityChart wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PublicTreasuryEntityChart>> publicTreasuryEntityChartAsync(
            final String entityId,
            final String coinId,
            final String days,
            final Boolean includeEmptyIntervals) {
        try {
            return preparePublicTreasuryEntityChartRequest(entityId, coinId, days,
            includeEmptyIntervals).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for publicTreasuryEntityChart.
     */
    private ApiCall<ApiResponse<PublicTreasuryEntityChart>, ApiException> preparePublicTreasuryEntityChartRequest(
            final String entityId,
            final String coinId,
            final String days,
            final Boolean includeEmptyIntervals) {
        return new ApiCall.Builder<ApiResponse<PublicTreasuryEntityChart>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/public_treasury/{entity_id}/{coin_id}/holding_chart")
                        .queryParam(param -> param.key("days")
                                .value(days))
                        .queryParam(param -> param.key("include_empty_intervals")
                                .value(includeEmptyIntervals).isRequired(false))
                        .templateParam(param -> param.key("entity_id").value(entityId)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("coin_id").value(coinId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PublicTreasuryEntityChart.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query public companies' and governments' cryptocurrency transaction history by entity ID.
     * @param  entityId  Required parameter: Public company or government entity ID. *refers to
     *         [`/entities/list`](/reference/entities-list).
     * @param  perPage  Optional parameter: Total results per page. Default value: 100 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  order  Optional parameter: Sort order of transactions. Default: `date_desc`
     * @param  coinIds  Optional parameter: Filter transactions by coin IDs, comma-separated if
     *         querying more than 1 coin. *refers to [`/coins/list`](/reference/coins-list).
     * @return    Returns the PublicTreasuryTransactionHistory wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PublicTreasuryTransactionHistory> publicTreasuryTransactionHistory(
            final String entityId,
            final Integer perPage,
            final Integer page,
            final Order6 order,
            final String coinIds) throws ApiException, IOException {
        return preparePublicTreasuryTransactionHistoryRequest(entityId, perPage, page, order,
                coinIds).execute();
    }

    /**
     * To query public companies' and governments' cryptocurrency transaction history by entity ID.
     * @param  entityId  Required parameter: Public company or government entity ID. *refers to
     *         [`/entities/list`](/reference/entities-list).
     * @param  perPage  Optional parameter: Total results per page. Default value: 100 Valid values:
     *         1...250
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  order  Optional parameter: Sort order of transactions. Default: `date_desc`
     * @param  coinIds  Optional parameter: Filter transactions by coin IDs, comma-separated if
     *         querying more than 1 coin. *refers to [`/coins/list`](/reference/coins-list).
     * @return    Returns the PublicTreasuryTransactionHistory wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PublicTreasuryTransactionHistory>> publicTreasuryTransactionHistoryAsync(
            final String entityId,
            final Integer perPage,
            final Integer page,
            final Order6 order,
            final String coinIds) {
        try {
            return preparePublicTreasuryTransactionHistoryRequest(entityId, perPage, page, order,
            coinIds).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for publicTreasuryTransactionHistory.
     */
    private ApiCall<ApiResponse<PublicTreasuryTransactionHistory>, ApiException> preparePublicTreasuryTransactionHistoryRequest(
            final String entityId,
            final Integer perPage,
            final Integer page,
            final Order6 order,
            final String coinIds) {
        return new ApiCall.Builder<ApiResponse<PublicTreasuryTransactionHistory>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/public_treasury/{entity_id}/transaction_history")
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("coin_ids")
                                .value(coinIds).isRequired(false))
                        .templateParam(param -> param.key("entity_id").value(entityId)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PublicTreasuryTransactionHistory.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all supported NFTs with ID, contract address, name, asset platform ID and symbol on
     * CoinGecko.
     * @param  order  Optional parameter: Sort order of responses.
     * @param  perPage  Optional parameter: Total results per page. Valid values: 1...250
     * @param  page  Optional parameter: Page through results.
     * @return    Returns the List of NfTsList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<List<NfTsList>> nftsList(
            final Order7 order,
            final Integer perPage,
            final Integer page) throws ApiException, IOException {
        return prepareNftsListRequest(order, perPage, page).execute();
    }

    /**
     * To query all supported NFTs with ID, contract address, name, asset platform ID and symbol on
     * CoinGecko.
     * @param  order  Optional parameter: Sort order of responses.
     * @param  perPage  Optional parameter: Total results per page. Valid values: 1...250
     * @param  page  Optional parameter: Page through results.
     * @return    Returns the List of NfTsList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<List<NfTsList>>> nftsListAsync(
            final Order7 order,
            final Integer perPage,
            final Integer page) {
        try {
            return prepareNftsListRequest(order, perPage, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for nftsList.
     */
    private ApiCall<ApiResponse<List<NfTsList>>, ApiException> prepareNftsListRequest(
            final Order7 order,
            final Integer perPage,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<List<NfTsList>>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/nfts/list")
                        .queryParam(param -> param.key("order")
                                .value((order != null) ? order.value() : null).isRequired(false))
                        .queryParam(param -> param.key("per_page")
                                .value(perPage).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserializeArray(response,
                                        NfTsList[].class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the NFT data (name, floor price, 24hr volume, ...) based on the NFT collection
     * ID.
     * @param  id  Required parameter: NFT collection ID. *refers to
     *         [`/nfts/list`](/reference/nfts-list).
     * @return    Returns the NftData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<NftData> nftsId(
            final String id) throws ApiException, IOException {
        return prepareNftsIdRequest(id).execute();
    }

    /**
     * To query all the NFT data (name, floor price, 24hr volume, ...) based on the NFT collection
     * ID.
     * @param  id  Required parameter: NFT collection ID. *refers to
     *         [`/nfts/list`](/reference/nfts-list).
     * @return    Returns the NftData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<NftData>> nftsIdAsync(
            final String id) {
        try {
            return prepareNftsIdRequest(id).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for nftsId.
     */
    private ApiCall<ApiResponse<NftData>, ApiException> prepareNftsIdRequest(
            final String id) {
        return new ApiCall.Builder<ApiResponse<NftData>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/nfts/{id}")
                        .templateParam(param -> param.key("id").value(id)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, NftData.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the NFT data (name, floor price, 24hr volume, ...) based on the NFT collection
     * contract address and respective asset platform.
     * @param  assetPlatformId  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: Contract address of the NFT collection.
     * @return    Returns the NftData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<NftData> nftsContractAddress(
            final String assetPlatformId,
            final String contractAddress) throws ApiException, IOException {
        return prepareNftsContractAddressRequest(assetPlatformId, contractAddress).execute();
    }

    /**
     * To query all the NFT data (name, floor price, 24hr volume, ...) based on the NFT collection
     * contract address and respective asset platform.
     * @param  assetPlatformId  Required parameter: Asset platform ID. *refers to
     *         [`/asset_platforms`](/reference/asset-platforms-list).
     * @param  contractAddress  Required parameter: Contract address of the NFT collection.
     * @return    Returns the NftData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<NftData>> nftsContractAddressAsync(
            final String assetPlatformId,
            final String contractAddress) {
        try {
            return prepareNftsContractAddressRequest(assetPlatformId, contractAddress).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for nftsContractAddress.
     */
    private ApiCall<ApiResponse<NftData>, ApiException> prepareNftsContractAddressRequest(
            final String assetPlatformId,
            final String contractAddress) {
        return new ApiCall.Builder<ApiResponse<NftData>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/nfts/{asset_platform_id}/contract/{contract_address}")
                        .templateParam(param -> param.key("asset_platform_id").value(assetPlatformId)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("contract_address").value(contractAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, NftData.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query BTC exchange rates with other currencies.
     * @return    Returns the ExchangeRates wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<ExchangeRates> exchangeRates() throws ApiException, IOException {
        return prepareExchangeRatesRequest().execute();
    }

    /**
     * To query BTC exchange rates with other currencies.
     * @return    Returns the ExchangeRates wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<ExchangeRates>> exchangeRatesAsync() {
        try {
            return prepareExchangeRatesRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for exchangeRates.
     */
    private ApiCall<ApiResponse<ExchangeRates>, ApiException> prepareExchangeRatesRequest() {
        return new ApiCall.Builder<ApiResponse<ExchangeRates>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/exchange_rates")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, ExchangeRates.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query trending search coins, NFTs and categories on CoinGecko in the last 24 hours.
     * @return    Returns the TrendingSearch wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TrendingSearch> trendingSearch() throws ApiException, IOException {
        return prepareTrendingSearchRequest().execute();
    }

    /**
     * To query trending search coins, NFTs and categories on CoinGecko in the last 24 hours.
     * @return    Returns the TrendingSearch wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TrendingSearch>> trendingSearchAsync() {
        try {
            return prepareTrendingSearchRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for trendingSearch.
     */
    private ApiCall<ApiResponse<TrendingSearch>, ApiException> prepareTrendingSearchRequest() {
        return new ApiCall.Builder<ApiResponse<TrendingSearch>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/search/trending")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TrendingSearch.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query cryptocurrency global data including active cryptocurrencies, markets, total crypto
     * market cap and etc.
     * @return    Returns the Global wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Global> cryptoGlobal() throws ApiException, IOException {
        return prepareCryptoGlobalRequest().execute();
    }

    /**
     * To query cryptocurrency global data including active cryptocurrencies, markets, total crypto
     * market cap and etc.
     * @return    Returns the Global wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Global>> cryptoGlobalAsync() {
        try {
            return prepareCryptoGlobalRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for cryptoGlobal.
     */
    private ApiCall<ApiResponse<Global>, ApiException> prepareCryptoGlobalRequest() {
        return new ApiCall.Builder<ApiResponse<Global>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/global")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Global.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query top 100 cryptocurrency global decentralized finance (DeFi) data including DeFi
     * market cap, trading volume.
     * @return    Returns the GlobalDeFi wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<GlobalDeFi> globalDefi() throws ApiException, IOException {
        return prepareGlobalDefiRequest().execute();
    }

    /**
     * To query top 100 cryptocurrency global decentralized finance (DeFi) data including DeFi
     * market cap, trading volume.
     * @return    Returns the GlobalDeFi wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<GlobalDeFi>> globalDefiAsync() {
        try {
            return prepareGlobalDefiRequest().executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for globalDefi.
     */
    private ApiCall<ApiResponse<GlobalDeFi>, ApiException> prepareGlobalDefiRequest() {
        return new ApiCall.Builder<ApiResponse<GlobalDeFi>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/global/decentralized_finance_defi")
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, GlobalDeFi.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the specific pool based on the provided network and pool address.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  address  Required parameter: Pool address.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  includeVolumeBreakdown  Optional parameter: Include volume breakdown. Default:
     *         `false`
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @return    Returns the PoolAddressData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PoolAddressData> poolAddress(
            final String network,
            final String address,
            final String include,
            final Boolean includeVolumeBreakdown,
            final Boolean includeComposition) throws ApiException, IOException {
        return preparePoolAddressRequest(network, address, include, includeVolumeBreakdown,
                includeComposition).execute();
    }

    /**
     * To query the specific pool based on the provided network and pool address.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  address  Required parameter: Pool address.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  includeVolumeBreakdown  Optional parameter: Include volume breakdown. Default:
     *         `false`
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @return    Returns the PoolAddressData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PoolAddressData>> poolAddressAsync(
            final String network,
            final String address,
            final String include,
            final Boolean includeVolumeBreakdown,
            final Boolean includeComposition) {
        try {
            return preparePoolAddressRequest(network, address, include, includeVolumeBreakdown,
            includeComposition).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for poolAddress.
     */
    private ApiCall<ApiResponse<PoolAddressData>, ApiException> preparePoolAddressRequest(
            final String network,
            final String address,
            final String include,
            final Boolean includeVolumeBreakdown,
            final Boolean includeComposition) {
        return new ApiCall.Builder<ApiResponse<PoolAddressData>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/pools/{address}")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("include_volume_breakdown")
                                .value(includeVolumeBreakdown).isRequired(false))
                        .queryParam(param -> param.key("include_composition")
                                .value(includeComposition).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("address").value(address)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PoolAddressData.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the trending pools across all networks on GeckoTerminal.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`, `network`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  duration  Optional parameter: Duration to sort trending list by. Default: `24h`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> trendingPoolsList(
            final String include,
            final Integer page,
            final Duration duration,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareTrendingPoolsListRequest(include, page, duration,
                includeGtCommunityData).execute();
    }

    /**
     * To query all the trending pools across all networks on GeckoTerminal.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`, `network`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  duration  Optional parameter: Duration to sort trending list by. Default: `24h`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> trendingPoolsListAsync(
            final String include,
            final Integer page,
            final Duration duration,
            final Boolean includeGtCommunityData) {
        try {
            return prepareTrendingPoolsListRequest(include, page, duration,
            includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for trendingPoolsList.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareTrendingPoolsListRequest(
            final String include,
            final Integer page,
            final Duration duration,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/trending_pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("duration")
                                .value((duration != null) ? duration.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the trending pools based on the provided network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  duration  Optional parameter: Duration to sort trending list by. Default: `24h`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> trendingPoolsNetwork(
            final String network,
            final String include,
            final Integer page,
            final Duration duration,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareTrendingPoolsNetworkRequest(network, include, page, duration,
                includeGtCommunityData).execute();
    }

    /**
     * To query the trending pools based on the provided network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  duration  Optional parameter: Duration to sort trending list by. Default: `24h`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> trendingPoolsNetworkAsync(
            final String network,
            final String include,
            final Integer page,
            final Duration duration,
            final Boolean includeGtCommunityData) {
        try {
            return prepareTrendingPoolsNetworkRequest(network, include, page, duration,
            includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for trendingPoolsNetwork.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareTrendingPoolsNetworkRequest(
            final String network,
            final String include,
            final Integer page,
            final Duration duration,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/trending_pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("duration")
                                .value((duration != null) ? duration.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the top pools based on the provided network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  sort  Optional parameter: Sort the pools by field. Default: `h24_tx_count_desc`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> topPoolsNetwork(
            final String network,
            final String include,
            final Integer page,
            final Sort sort,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareTopPoolsNetworkRequest(network, include, page, sort,
                includeGtCommunityData).execute();
    }

    /**
     * To query all the top pools based on the provided network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  sort  Optional parameter: Sort the pools by field. Default: `h24_tx_count_desc`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> topPoolsNetworkAsync(
            final String network,
            final String include,
            final Integer page,
            final Sort sort,
            final Boolean includeGtCommunityData) {
        try {
            return prepareTopPoolsNetworkRequest(network, include, page, sort,
            includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for topPoolsNetwork.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareTopPoolsNetworkRequest(
            final String network,
            final String include,
            final Integer page,
            final Sort sort,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value((sort != null) ? sort.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the top pools based on the provided network and decentralized exchange (DEX).
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  dex  Required parameter: DEX ID. *refers to
     *         [`/onchain/networks/{network}/dexes`](/reference/dexes-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  sort  Optional parameter: Sort the pools by field. Default: `h24_tx_count_desc`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> topPoolsDex(
            final String network,
            final String dex,
            final String include,
            final Integer page,
            final Sort sort,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareTopPoolsDexRequest(network, dex, include, page, sort,
                includeGtCommunityData).execute();
    }

    /**
     * To query all the top pools based on the provided network and decentralized exchange (DEX).
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  dex  Required parameter: DEX ID. *refers to
     *         [`/onchain/networks/{network}/dexes`](/reference/dexes-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  sort  Optional parameter: Sort the pools by field. Default: `h24_tx_count_desc`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> topPoolsDexAsync(
            final String network,
            final String dex,
            final String include,
            final Integer page,
            final Sort sort,
            final Boolean includeGtCommunityData) {
        try {
            return prepareTopPoolsDexRequest(network, dex, include, page, sort,
            includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for topPoolsDex.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareTopPoolsDexRequest(
            final String network,
            final String dex,
            final String include,
            final Integer page,
            final Sort sort,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/dexes/{dex}/pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value((sort != null) ? sort.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("dex").value(dex)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query top pools based on the provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  tokenAddress  Required parameter: Token contract address.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  includeInactiveSource  Optional parameter: Include tokens from inactive pools using
     *         the most recent swap. Default: `false`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  sort  Optional parameter: Sort the pools by field. Default:
     *         `h24_volume_usd_liquidity_desc`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> topPoolsContractAddress(
            final String network,
            final String tokenAddress,
            final String include,
            final Boolean includeInactiveSource,
            final Integer page,
            final Sort2 sort,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareTopPoolsContractAddressRequest(network, tokenAddress, include,
                includeInactiveSource, page, sort, includeGtCommunityData).execute();
    }

    /**
     * To query top pools based on the provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  tokenAddress  Required parameter: Token contract address.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  includeInactiveSource  Optional parameter: Include tokens from inactive pools using
     *         the most recent swap. Default: `false`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  sort  Optional parameter: Sort the pools by field. Default:
     *         `h24_volume_usd_liquidity_desc`
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> topPoolsContractAddressAsync(
            final String network,
            final String tokenAddress,
            final String include,
            final Boolean includeInactiveSource,
            final Integer page,
            final Sort2 sort,
            final Boolean includeGtCommunityData) {
        try {
            return prepareTopPoolsContractAddressRequest(network, tokenAddress, include,
            includeInactiveSource, page, sort, includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for topPoolsContractAddress.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareTopPoolsContractAddressRequest(
            final String network,
            final String tokenAddress,
            final String include,
            final Boolean includeInactiveSource,
            final Integer page,
            final Sort2 sort,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/tokens/{token_address}/pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("include_inactive_source")
                                .value(includeInactiveSource).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("sort")
                                .value((sort != null) ? sort.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("token_address").value(tokenAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query specific token data based on the provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  address  Required parameter: Token contract address.
     * @param  include  Optional parameter: Attributes to include.
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @param  includeInactiveSource  Optional parameter: Include token data from inactive pools
     *         using the most recent swap. Default: `false`
     * @return    Returns the TokenData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TokenData> tokenDataContractAddress(
            final String network,
            final String address,
            final Include include,
            final Boolean includeComposition,
            final Boolean includeInactiveSource) throws ApiException, IOException {
        return prepareTokenDataContractAddressRequest(network, address, include, includeComposition,
                includeInactiveSource).execute();
    }

    /**
     * To query specific token data based on the provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  address  Required parameter: Token contract address.
     * @param  include  Optional parameter: Attributes to include.
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @param  includeInactiveSource  Optional parameter: Include token data from inactive pools
     *         using the most recent swap. Default: `false`
     * @return    Returns the TokenData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TokenData>> tokenDataContractAddressAsync(
            final String network,
            final String address,
            final Include include,
            final Boolean includeComposition,
            final Boolean includeInactiveSource) {
        try {
            return prepareTokenDataContractAddressRequest(network, address, include, includeComposition,
            includeInactiveSource).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for tokenDataContractAddress.
     */
    private ApiCall<ApiResponse<TokenData>, ApiException> prepareTokenDataContractAddressRequest(
            final String network,
            final String address,
            final Include include,
            final Boolean includeComposition,
            final Boolean includeInactiveSource) {
        return new ApiCall.Builder<ApiResponse<TokenData>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/tokens/{address}")
                        .queryParam(param -> param.key("include")
                                .value((include != null) ? include.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_composition")
                                .value(includeComposition).isRequired(false))
                        .queryParam(param -> param.key("include_inactive_source")
                                .value(includeInactiveSource).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("address").value(address)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TokenData.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query multiple tokens data based on the provided token contract addresses on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  addresses  Required parameter: Token contract address, comma-separated if more than
     *         one token contract address.
     * @param  include  Optional parameter: Attributes to include.
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @param  includeInactiveSource  Optional parameter: Include tokens from inactive pools using
     *         the most recent swap. Default: `false`
     * @return    Returns the MultiTokenData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<MultiTokenData> tokensDataContractAddresses(
            final String network,
            final String addresses,
            final Include include,
            final Boolean includeComposition,
            final Boolean includeInactiveSource) throws ApiException, IOException {
        return prepareTokensDataContractAddressesRequest(network, addresses, include,
                includeComposition, includeInactiveSource).execute();
    }

    /**
     * To query multiple tokens data based on the provided token contract addresses on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  addresses  Required parameter: Token contract address, comma-separated if more than
     *         one token contract address.
     * @param  include  Optional parameter: Attributes to include.
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @param  includeInactiveSource  Optional parameter: Include tokens from inactive pools using
     *         the most recent swap. Default: `false`
     * @return    Returns the MultiTokenData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<MultiTokenData>> tokensDataContractAddressesAsync(
            final String network,
            final String addresses,
            final Include include,
            final Boolean includeComposition,
            final Boolean includeInactiveSource) {
        try {
            return prepareTokensDataContractAddressesRequest(network, addresses, include,
            includeComposition, includeInactiveSource).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for tokensDataContractAddresses.
     */
    private ApiCall<ApiResponse<MultiTokenData>, ApiException> prepareTokensDataContractAddressesRequest(
            final String network,
            final String addresses,
            final Include include,
            final Boolean includeComposition,
            final Boolean includeInactiveSource) {
        return new ApiCall.Builder<ApiResponse<MultiTokenData>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/tokens/multi/{addresses}")
                        .queryParam(param -> param.key("include")
                                .value((include != null) ? include.value() : null).isRequired(false))
                        .queryParam(param -> param.key("include_composition")
                                .value(includeComposition).isRequired(false))
                        .queryParam(param -> param.key("include_inactive_source")
                                .value(includeInactiveSource).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("addresses").value(addresses)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, MultiTokenData.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query token metadata (name, symbol, CoinGecko ID, image, socials, websites, description,
     * etc.) based on a provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  address  Required parameter: Token contract address.
     * @return    Returns the TokenInfo wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TokenInfo> tokenInfoContractAddress(
            final String network,
            final String address) throws ApiException, IOException {
        return prepareTokenInfoContractAddressRequest(network, address).execute();
    }

    /**
     * To query token metadata (name, symbol, CoinGecko ID, image, socials, websites, description,
     * etc.) based on a provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  address  Required parameter: Token contract address.
     * @return    Returns the TokenInfo wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TokenInfo>> tokenInfoContractAddressAsync(
            final String network,
            final String address) {
        try {
            return prepareTokenInfoContractAddressRequest(network, address).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for tokenInfoContractAddress.
     */
    private ApiCall<ApiResponse<TokenInfo>, ApiException> prepareTokenInfoContractAddressRequest(
            final String network,
            final String address) {
        return new ApiCall.Builder<ApiResponse<TokenInfo>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/tokens/{address}/info")
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("address").value(address)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TokenInfo.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query pool metadata (base and quote token details, image, socials, websites, description,
     * contract address, etc.) based on a provided pool contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  poolAddress  Required parameter: Pool contract address.
     * @param  include  Optional parameter: Attributes to include.
     * @return    Returns the PoolTokensInfo wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PoolTokensInfo> poolTokenInfoContractAddress(
            final String network,
            final String poolAddress,
            final Include2 include) throws ApiException, IOException {
        return preparePoolTokenInfoContractAddressRequest(network, poolAddress, include).execute();
    }

    /**
     * To query pool metadata (base and quote token details, image, socials, websites, description,
     * contract address, etc.) based on a provided pool contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  poolAddress  Required parameter: Pool contract address.
     * @param  include  Optional parameter: Attributes to include.
     * @return    Returns the PoolTokensInfo wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PoolTokensInfo>> poolTokenInfoContractAddressAsync(
            final String network,
            final String poolAddress,
            final Include2 include) {
        try {
            return preparePoolTokenInfoContractAddressRequest(network, poolAddress, include).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for poolTokenInfoContractAddress.
     */
    private ApiCall<ApiResponse<PoolTokensInfo>, ApiException> preparePoolTokenInfoContractAddressRequest(
            final String network,
            final String poolAddress,
            final Include2 include) {
        return new ApiCall.Builder<ApiResponse<PoolTokensInfo>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/pools/{pool_address}/info")
                        .queryParam(param -> param.key("include")
                                .value((include != null) ? include.value() : null).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("pool_address").value(poolAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PoolTokensInfo.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query 100 most recently updated tokens info of a specific network or across all networks
     * on GeckoTerminal.
     * @param  include  Optional parameter: Attributes for related resources to include.
     * @param  network  Optional parameter: Filter tokens by provided network. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @return    Returns the TokenInfoRecentlyUpdated wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<TokenInfoRecentlyUpdated> tokensInfoRecentUpdated(
            final Include3 include,
            final String network) throws ApiException, IOException {
        return prepareTokensInfoRecentUpdatedRequest(include, network).execute();
    }

    /**
     * To query 100 most recently updated tokens info of a specific network or across all networks
     * on GeckoTerminal.
     * @param  include  Optional parameter: Attributes for related resources to include.
     * @param  network  Optional parameter: Filter tokens by provided network. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @return    Returns the TokenInfoRecentlyUpdated wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<TokenInfoRecentlyUpdated>> tokensInfoRecentUpdatedAsync(
            final Include3 include,
            final String network) {
        try {
            return prepareTokensInfoRecentUpdatedRequest(include, network).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for tokensInfoRecentUpdated.
     */
    private ApiCall<ApiResponse<TokenInfoRecentlyUpdated>, ApiException> prepareTokensInfoRecentUpdatedRequest(
            final Include3 include,
            final String network) {
        return new ApiCall.Builder<ApiResponse<TokenInfoRecentlyUpdated>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/tokens/info_recently_updated")
                        .queryParam(param -> param.key("include")
                                .value((include != null) ? include.value() : null).isRequired(false))
                        .queryParam(param -> param.key("network")
                                .value(network).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, TokenInfoRecentlyUpdated.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get the OHLCV chart (Open, High, Low, Close, Volume) of a pool based on the provided pool
     * address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  poolAddress  Required parameter: Pool contract address.
     * @param  timeframe  Required parameter: Timeframe of the OHLCV chart.
     * @param  aggregate  Optional parameter: Time period to aggregate each OHLCV. Available values
     *         (day): `1` Available values (hour): `1`, `4`, `12` Available values (minute): `1`,
     *         `5`, `15` Default value: 1
     * @param  beforeTimestamp  Optional parameter: Return OHLCV data before this timestamp (integer
     *         seconds since epoch).
     * @param  limit  Optional parameter: Number of OHLCV results to return, maximum 1000. Default
     *         value: 100
     * @param  currency  Optional parameter: Return OHLCV in USD or quote token. Default: `usd`
     * @param  token  Optional parameter: Return OHLCV for token, use this to invert the chart.
     *         Available values: `base`, `quote`, or token address. Default: `base`
     * @param  includeEmptyIntervals  Optional parameter: Include empty intervals with no trade
     *         data. Default: `false`
     * @return    Returns the Ohlcv wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Ohlcv> poolOhlcvContractAddress(
            final String network,
            final String poolAddress,
            final Timeframe timeframe,
            final String aggregate,
            final Integer beforeTimestamp,
            final Integer limit,
            final Currency currency,
            final String token,
            final Boolean includeEmptyIntervals) throws ApiException, IOException {
        return preparePoolOhlcvContractAddressRequest(network, poolAddress, timeframe, aggregate,
                beforeTimestamp, limit, currency, token, includeEmptyIntervals).execute();
    }

    /**
     * To get the OHLCV chart (Open, High, Low, Close, Volume) of a pool based on the provided pool
     * address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  poolAddress  Required parameter: Pool contract address.
     * @param  timeframe  Required parameter: Timeframe of the OHLCV chart.
     * @param  aggregate  Optional parameter: Time period to aggregate each OHLCV. Available values
     *         (day): `1` Available values (hour): `1`, `4`, `12` Available values (minute): `1`,
     *         `5`, `15` Default value: 1
     * @param  beforeTimestamp  Optional parameter: Return OHLCV data before this timestamp (integer
     *         seconds since epoch).
     * @param  limit  Optional parameter: Number of OHLCV results to return, maximum 1000. Default
     *         value: 100
     * @param  currency  Optional parameter: Return OHLCV in USD or quote token. Default: `usd`
     * @param  token  Optional parameter: Return OHLCV for token, use this to invert the chart.
     *         Available values: `base`, `quote`, or token address. Default: `base`
     * @param  includeEmptyIntervals  Optional parameter: Include empty intervals with no trade
     *         data. Default: `false`
     * @return    Returns the Ohlcv wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Ohlcv>> poolOhlcvContractAddressAsync(
            final String network,
            final String poolAddress,
            final Timeframe timeframe,
            final String aggregate,
            final Integer beforeTimestamp,
            final Integer limit,
            final Currency currency,
            final String token,
            final Boolean includeEmptyIntervals) {
        try {
            return preparePoolOhlcvContractAddressRequest(network, poolAddress, timeframe, aggregate,
            beforeTimestamp, limit, currency, token, includeEmptyIntervals).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for poolOhlcvContractAddress.
     */
    private ApiCall<ApiResponse<Ohlcv>, ApiException> preparePoolOhlcvContractAddressRequest(
            final String network,
            final String poolAddress,
            final Timeframe timeframe,
            final String aggregate,
            final Integer beforeTimestamp,
            final Integer limit,
            final Currency currency,
            final String token,
            final Boolean includeEmptyIntervals) {
        return new ApiCall.Builder<ApiResponse<Ohlcv>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/pools/{pool_address}/ohlcv/{timeframe}")
                        .queryParam(param -> param.key("aggregate")
                                .value(aggregate).isRequired(false))
                        .queryParam(param -> param.key("before_timestamp")
                                .value(beforeTimestamp).isRequired(false))
                        .queryParam(param -> param.key("limit")
                                .value(limit).isRequired(false))
                        .queryParam(param -> param.key("currency")
                                .value((currency != null) ? currency.value() : null).isRequired(false))
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .queryParam(param -> param.key("include_empty_intervals")
                                .value(includeEmptyIntervals).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("pool_address").value(poolAddress)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("timeframe").value((timeframe != null) ? timeframe.value() : "day")
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Ohlcv.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query the last 300 trades in the past 24 hours based on the provided pool address.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  poolAddress  Required parameter: Pool contract address.
     * @param  tradeVolumeInUsdGreaterThan  Optional parameter: Filter trades by trade volume in USD
     *         greater than this value. Default value: 0
     * @param  token  Optional parameter: Return trades for token, use this to invert the chart.
     *         Available values: `base`, `quote`, or token address. Default: `base`
     * @return    Returns the Trades wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Trades> poolTradesContractAddress(
            final String network,
            final String poolAddress,
            final Double tradeVolumeInUsdGreaterThan,
            final String token) throws ApiException, IOException {
        return preparePoolTradesContractAddressRequest(network, poolAddress,
                tradeVolumeInUsdGreaterThan, token).execute();
    }

    /**
     * To query the last 300 trades in the past 24 hours based on the provided pool address.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  poolAddress  Required parameter: Pool contract address.
     * @param  tradeVolumeInUsdGreaterThan  Optional parameter: Filter trades by trade volume in USD
     *         greater than this value. Default value: 0
     * @param  token  Optional parameter: Return trades for token, use this to invert the chart.
     *         Available values: `base`, `quote`, or token address. Default: `base`
     * @return    Returns the Trades wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Trades>> poolTradesContractAddressAsync(
            final String network,
            final String poolAddress,
            final Double tradeVolumeInUsdGreaterThan,
            final String token) {
        try {
            return preparePoolTradesContractAddressRequest(network, poolAddress,
            tradeVolumeInUsdGreaterThan, token).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for poolTradesContractAddress.
     */
    private ApiCall<ApiResponse<Trades>, ApiException> preparePoolTradesContractAddressRequest(
            final String network,
            final String poolAddress,
            final Double tradeVolumeInUsdGreaterThan,
            final String token) {
        return new ApiCall.Builder<ApiResponse<Trades>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/pools/{pool_address}/trades")
                        .queryParam(param -> param.key("trade_volume_in_usd_greater_than")
                                .value(tradeVolumeInUsdGreaterThan).isRequired(false))
                        .queryParam(param -> param.key("token")
                                .value(token).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("pool_address").value(poolAddress)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Trades.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the latest pools across all networks on GeckoTerminal.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`, `network`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> latestPoolsList(
            final String include,
            final Integer page,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareLatestPoolsListRequest(include, page, includeGtCommunityData).execute();
    }

    /**
     * To query all the latest pools across all networks on GeckoTerminal.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`, `network`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> latestPoolsListAsync(
            final String include,
            final Integer page,
            final Boolean includeGtCommunityData) {
        try {
            return prepareLatestPoolsListRequest(include, page, includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for latestPoolsList.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareLatestPoolsListRequest(
            final String include,
            final Integer page,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/new_pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the latest pools based on the provided network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<Pool> latestPoolsNetwork(
            final String network,
            final String include,
            final Integer page,
            final Boolean includeGtCommunityData) throws ApiException, IOException {
        return prepareLatestPoolsNetworkRequest(network, include, page,
                includeGtCommunityData).execute();
    }

    /**
     * To query all the latest pools based on the provided network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @param  includeGtCommunityData  Optional parameter: Include GeckoTerminal community data
     *         (sentiment votes, suspicious reports). Default: `false`
     * @return    Returns the Pool wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<Pool>> latestPoolsNetworkAsync(
            final String network,
            final String include,
            final Integer page,
            final Boolean includeGtCommunityData) {
        try {
            return prepareLatestPoolsNetworkRequest(network, include, page,
            includeGtCommunityData).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for latestPoolsNetwork.
     */
    private ApiCall<ApiResponse<Pool>, ApiException> prepareLatestPoolsNetworkRequest(
            final String network,
            final String include,
            final Integer page,
            final Boolean includeGtCommunityData) {
        return new ApiCall.Builder<ApiResponse<Pool>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/new_pools")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .queryParam(param -> param.key("include_gt_community_data")
                                .value(includeGtCommunityData).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, Pool.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query multiple pools based on the provided network and pool addresses.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  addresses  Required parameter: Pool contract address, comma-separated if more than
     *         one pool contract address.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  includeVolumeBreakdown  Optional parameter: Include volume breakdown. Default:
     *         `false`
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @return    Returns the MultiPoolAddressData wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<MultiPoolAddressData> poolsAddresses(
            final String network,
            final String addresses,
            final String include,
            final Boolean includeVolumeBreakdown,
            final Boolean includeComposition) throws ApiException, IOException {
        return preparePoolsAddressesRequest(network, addresses, include, includeVolumeBreakdown,
                includeComposition).execute();
    }

    /**
     * To query multiple pools based on the provided network and pool addresses.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  addresses  Required parameter: Pool contract address, comma-separated if more than
     *         one pool contract address.
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  includeVolumeBreakdown  Optional parameter: Include volume breakdown. Default:
     *         `false`
     * @param  includeComposition  Optional parameter: Include pool composition. Default: `false`
     * @return    Returns the MultiPoolAddressData wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<MultiPoolAddressData>> poolsAddressesAsync(
            final String network,
            final String addresses,
            final String include,
            final Boolean includeVolumeBreakdown,
            final Boolean includeComposition) {
        try {
            return preparePoolsAddressesRequest(network, addresses, include, includeVolumeBreakdown,
            includeComposition).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for poolsAddresses.
     */
    private ApiCall<ApiResponse<MultiPoolAddressData>, ApiException> preparePoolsAddressesRequest(
            final String network,
            final String addresses,
            final String include,
            final Boolean includeVolumeBreakdown,
            final Boolean includeComposition) {
        return new ApiCall.Builder<ApiResponse<MultiPoolAddressData>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/pools/multi/{addresses}")
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("include_volume_breakdown")
                                .value(includeVolumeBreakdown).isRequired(false))
                        .queryParam(param -> param.key("include_composition")
                                .value(includeComposition).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("addresses").value(addresses)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, MultiPoolAddressData.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To search for pools across all networks by pool address, token name, token symbol, or token
     * contract address.
     * @param  query  Optional parameter: Search query: pool contract address, token name, token
     *         symbol, or token contract address.
     * @param  network  Optional parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the PoolSearch wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<PoolSearch> searchPools(
            final String query,
            final String network,
            final String include,
            final Integer page) throws ApiException, IOException {
        return prepareSearchPoolsRequest(query, network, include, page).execute();
    }

    /**
     * To search for pools across all networks by pool address, token name, token symbol, or token
     * contract address.
     * @param  query  Optional parameter: Search query: pool contract address, token name, token
     *         symbol, or token contract address.
     * @param  network  Optional parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  include  Optional parameter: Attributes to include, comma-separated if more than one.
     *         Available values: `base_token`, `quote_token`, `dex`
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the PoolSearch wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<PoolSearch>> searchPoolsAsync(
            final String query,
            final String network,
            final String include,
            final Integer page) {
        try {
            return prepareSearchPoolsRequest(query, network, include, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for searchPools.
     */
    private ApiCall<ApiResponse<PoolSearch>, ApiException> prepareSearchPoolsRequest(
            final String query,
            final String network,
            final String include,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<PoolSearch>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/search/pools")
                        .queryParam(param -> param.key("query")
                                .value((query != null) ? query : "weth").isRequired(false))
                        .queryParam(param -> param.key("network")
                                .value(network).isRequired(false))
                        .queryParam(param -> param.key("include")
                                .value(include).isRequired(false))
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, PoolSearch.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To get token price based on the provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  addresses  Required parameter: Token contract address, comma-separated if more than
     *         one token contract address.
     * @param  includeMarketCap  Optional parameter: Include market capitalization. Default: `false`
     * @param  mcapFdvFallback  Optional parameter: Return FDV if market cap is not available.
     *         Default: `false`
     * @param  include24HrVol  Optional parameter: Include 24hr volume. Default: `false`
     * @param  include24HrPriceChange  Optional parameter: Include 24hr price change. Default:
     *         `false`
     * @param  includeTotalReserveInUsd  Optional parameter: Include total reserve in USD. Default:
     *         `false`
     * @param  includeInactiveSource  Optional parameter: Include token price data from inactive
     *         pools using the most recent swap. Default: `false`
     * @return    Returns the OnchainSimplePrice wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<OnchainSimplePrice> onchainSimplePrice(
            final String network,
            final String addresses,
            final Boolean includeMarketCap,
            final Boolean mcapFdvFallback,
            final Boolean include24HrVol,
            final Boolean include24HrPriceChange,
            final Boolean includeTotalReserveInUsd,
            final Boolean includeInactiveSource) throws ApiException, IOException {
        return prepareOnchainSimplePriceRequest(network, addresses, includeMarketCap,
                mcapFdvFallback, include24HrVol, include24HrPriceChange, includeTotalReserveInUsd,
                includeInactiveSource).execute();
    }

    /**
     * To get token price based on the provided token contract address on a network.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  addresses  Required parameter: Token contract address, comma-separated if more than
     *         one token contract address.
     * @param  includeMarketCap  Optional parameter: Include market capitalization. Default: `false`
     * @param  mcapFdvFallback  Optional parameter: Return FDV if market cap is not available.
     *         Default: `false`
     * @param  include24HrVol  Optional parameter: Include 24hr volume. Default: `false`
     * @param  include24HrPriceChange  Optional parameter: Include 24hr price change. Default:
     *         `false`
     * @param  includeTotalReserveInUsd  Optional parameter: Include total reserve in USD. Default:
     *         `false`
     * @param  includeInactiveSource  Optional parameter: Include token price data from inactive
     *         pools using the most recent swap. Default: `false`
     * @return    Returns the OnchainSimplePrice wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<OnchainSimplePrice>> onchainSimplePriceAsync(
            final String network,
            final String addresses,
            final Boolean includeMarketCap,
            final Boolean mcapFdvFallback,
            final Boolean include24HrVol,
            final Boolean include24HrPriceChange,
            final Boolean includeTotalReserveInUsd,
            final Boolean includeInactiveSource) {
        try {
            return prepareOnchainSimplePriceRequest(network, addresses, includeMarketCap, mcapFdvFallback,
            include24HrVol, include24HrPriceChange, includeTotalReserveInUsd,
            includeInactiveSource).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for onchainSimplePrice.
     */
    private ApiCall<ApiResponse<OnchainSimplePrice>, ApiException> prepareOnchainSimplePriceRequest(
            final String network,
            final String addresses,
            final Boolean includeMarketCap,
            final Boolean mcapFdvFallback,
            final Boolean include24HrVol,
            final Boolean include24HrPriceChange,
            final Boolean includeTotalReserveInUsd,
            final Boolean includeInactiveSource) {
        return new ApiCall.Builder<ApiResponse<OnchainSimplePrice>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/simple/networks/{network}/token_price/{addresses}")
                        .queryParam(param -> param.key("include_market_cap")
                                .value(includeMarketCap).isRequired(false))
                        .queryParam(param -> param.key("mcap_fdv_fallback")
                                .value(mcapFdvFallback).isRequired(false))
                        .queryParam(param -> param.key("include_24hr_vol")
                                .value(include24HrVol).isRequired(false))
                        .queryParam(param -> param.key("include_24hr_price_change")
                                .value(include24HrPriceChange).isRequired(false))
                        .queryParam(param -> param.key("include_total_reserve_in_usd")
                                .value(includeTotalReserveInUsd).isRequired(false))
                        .queryParam(param -> param.key("include_inactive_source")
                                .value(includeInactiveSource).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .templateParam(param -> param.key("addresses").value(addresses)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, OnchainSimplePrice.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To retrieve a list of all supported networks on GeckoTerminal.
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the NetworksList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<NetworksList> networksList(
            final Integer page) throws ApiException, IOException {
        return prepareNetworksListRequest(page).execute();
    }

    /**
     * To retrieve a list of all supported networks on GeckoTerminal.
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the NetworksList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<NetworksList>> networksListAsync(
            final Integer page) {
        try {
            return prepareNetworksListRequest(page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for networksList.
     */
    private ApiCall<ApiResponse<NetworksList>, ApiException> prepareNetworksListRequest(
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<NetworksList>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks")
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, NetworksList.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }

    /**
     * To query all the supported decentralized exchanges (DEXs) based on the provided network on
     * GeckoTerminal.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the DexesList wrapped in ApiResponse response from the API call
     * @throws    ApiException    Represents error response from the server.
     * @throws    IOException    Signals that an I/O exception of some sort has occurred.
     */
    public ApiResponse<DexesList> dexesList(
            final String network,
            final Integer page) throws ApiException, IOException {
        return prepareDexesListRequest(network, page).execute();
    }

    /**
     * To query all the supported decentralized exchanges (DEXs) based on the provided network on
     * GeckoTerminal.
     * @param  network  Required parameter: Network ID. *refers to
     *         [`/onchain/networks`](/reference/networks-list).
     * @param  page  Optional parameter: Page through results. Default value: 1
     * @return    Returns the DexesList wrapped in ApiResponse response from the API call
     */
    public CompletableFuture<ApiResponse<DexesList>> dexesListAsync(
            final String network,
            final Integer page) {
        try {
            return prepareDexesListRequest(network, page).executeAsync();
        } catch (Exception e) {
            throw new CompletionException(e);
        }
    }

    /**
     * Builds the ApiCall object for dexesList.
     */
    private ApiCall<ApiResponse<DexesList>, ApiException> prepareDexesListRequest(
            final String network,
            final Integer page) {
        return new ApiCall.Builder<ApiResponse<DexesList>, ApiException>()
                .globalConfig(getGlobalConfiguration())
                .requestBuilder(requestBuilder -> requestBuilder
                        .server(Server.ENUM_DEFAULT.value())
                        .path("/onchain/networks/{network}/dexes")
                        .queryParam(param -> param.key("page")
                                .value(page).isRequired(false))
                        .templateParam(param -> param.key("network").value(network)
                                .shouldEncode(true))
                        .headerParam(param -> param.key("accept").value("application/json"))
                        .withAuth(auth -> auth
                                .or(orAuth -> orAuth
                                        .add("headerAuth")
                                        .add("queryAuth")))
                        .httpMethod(HttpMethod.GET))
                .responseHandler(responseHandler -> responseHandler
                        .responseClassType(ResponseClassType.API_RESPONSE)
                        .apiResponseDeserializer(
                                response -> ApiHelper.deserialize(response, DexesList.class))
                        .nullify404(false)
                        .globalErrorCase(GLOBAL_ERROR_CASES))
                .build();
    }
}